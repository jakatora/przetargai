# Audyt PrzetargAI — rundy 21–25 (2026-07-10)

## [CRITICAL] mobile/src/config.js:17

**Produkcyjny API_URL wskazuje Railway, który po migracji nie obsługuje już przetargai — apka jest w 100% martwa**

- Scenariusz: W buildzie produkcyjnym __DEV__ jest false, więc API_URL zawsze = 'https://backend-production-a43e3.up.railway.app'. Klient (mobile/src/api/client.js) woła API_URL + '/matches', '/auth/login', '/auth/register' itd. Tymczasem backend Railway (backend/src/app.js) montuje TYLKO trasy '/api/fitter/*' — trasy przetargai (auth, matches, upgrade) zostały przeniesione na Cloud Functions. Efekt: każde żądanie z apki dostaje 404. Użytkownik nie zaloguje się, nie zarejestruje, nie zobaczy dopasowań — aplikacja jest całkowicie niefunkcjonalna po publikacji.
- Naprawa: Zmień produkcyjną gałąź na adres funkcji `api`: 'https://europe-central2-przetargai.cloudfunctions.net/api' (dokładnie to samo co domyślne APP_URL w firebase/functions/src/config.js). Express w funkcji montuje trasy na '/matches','/auth' bez prefiksu, a Cloud Functions v2 zdejmuje nazwę funkcji, więc API_URL + '/matches' = .../api/matches trafi poprawnie. Docelowo po podpięciu custom domain: https://api.przetargai.pl. Zaktualizuj też komentarz mówiący o Railway.

## [CRITICAL] mobile/src/config.js:19

**Produkcyjny API_URL wciąż wskazuje martwy backend Railway — po migracji na Firebase cała apka nie działa**

- Scenariusz: W buildzie release (__DEV__ === false) klient REST (mobile/src/api/client.js:27) uderza w 'https://backend-production-a43e3.up.railway.app'. Ten backend obsługuje już tylko Fittera — trasy /auth/*, /matches/* dla przetargai tam nie istnieją. Prawdziwy adres Functions to 'https://europe-central2-przetargai.cloudfunctions.net/api' (region z firebase/functions/index.js:16, eksport 'api' w index.js:66; potwierdza to też APP_URL w firebase/functions/src/config.js). Efekt: po instalacji ze sklepu każda rejestracja/logowanie/pobranie dopasowań zwraca 404 lub trafia do obcej usługi — recenzent Apple/Google loguje się demo-kontem i dostaje błąd → gwarantowane odrzucenie, a realny user ma apkę nie do użycia.
- Naprawa: Zmienić gałąź PROD na URL Functions: 'https://europe-central2-przetargai.cloudfunctions.net/api' (albo custom domain https://api.przetargai.pl po podpięciu). Zbudować i przetestować release przed wysłaniem do sklepów.

## [CRITICAL] mobile/src/config.js:19

**Aplikacja produkcyjna uderza w Railway, a backend PrzetargAI jest już na Firebase — całkowicie martwa apka po publikacji**

- Scenariusz: API_URL w gałęzi produkcyjnej to 'https://backend-production-a43e3.up.railway.app', ale po migracji D-024 całe API przetargai żyje w funkcji HTTPS `api` (europe-central2, projekt przetargai), a Railway obsługuje już tylko Fittera. Klient robi fetch(API_URL + '/auth/login' | '/matches' ...). Po zbudowaniu release'u KAŻDE żądanie idzie na Railway: albo 404 (Railway nie ma tych tras bez prefiksu /api/przetargai), albo trafia w martwą, zdeprecjonowaną instancję. Użytkownik nie zaloguje się, nie zobaczy dopasowań — apka jest niefunkcjonalna od pierwszego ekranu. O 3 w nocy diagnoza trywialna (wszystko 404/timeout na jeden host), ale to twardy bloker wypuszczenia.
- Naprawa: Zmień gałąź produkcyjną na realny publiczny URL wdrożonej funkcji `api` (gen2 onRequest — sprawdź w konsoli Firebase, to zwykle *.run.app albo alias europe-central2-przetargai.cloudfunctions.net/api). Najlepiej nie hardkoduj — czytaj z app.json extra, żeby DEV/PROD nie rozjeżdżały się przy każdej migracji. Zweryfikuj, że ścieżki bez prefiksu /api pasują do mountów Express (app.use('/auth', ...)).

## [HIGH] firebase/functions/src/routes/webhooks.js:153

**Nieudana faktura Fakturowni NIE zwalnia rezerwacji — klient płaci, faktury VAT nie dostaje nigdy**

- Scenariusz: wystawFakture() zakłada, że createStandardInvoice() RZUCA przy błędzie i wtedy blok catch woła faktury.zwolnij(session.id). Ale createStandardInvoice() (invoice.js:38-56) łapie wszystko wewnętrznie i przy HTTP 500 / timeoncie / trybie degradacji ZWRACA { created:false } zamiast rzucać. Ścieżka: klient płaci 199 zł -> checkout.session.completed (payment_status='paid') -> zarezerwuj(session.id) tworzy invoices/{session.id} (true) -> Fakturownia zwraca 500 -> createStandardInvoice zwraca {created:false} bez wyjątku -> catch w wystawFakture NIE odpala -> rezerwacja ZOSTAJE. handleEvent kończy się bez wyjątku, więc markDone domyka zdarzenie i Stripe nigdy nie ponawia. Nawet gdyby ponowił, zarezerwuj(session.id) zwróci false ('Faktura dla tej sesji już istnieje — pomijam'). Faktura VAT nie zostaje wystawiona nigdy, mimo że e-mail aktywacyjny do klienta już poszedł. Naruszenie obowiązku wystawienia faktury (PL), po cichu.
- Naprawa: Sprawdzać wynik createStandardInvoice zamiast polegać na wyjątku: const wynik = await createStandardInvoice(...); if (!wynik.created && !wynik.degraded) { await faktury.zwolnij(session.id); throw ... } — albo zmienić createStandardInvoice, by rzucała przy błędach nadających się do ponowienia (HTTP>=500, timeout), a wystawFakture, by zwalniała rezerwację i propagowała błąd (zwrot 500 z webhooka, żeby Stripe ponowił). Tryb degradacji (features.invoicing=false) też musi zwolnić rezerwację, inaczej po skonfigurowaniu Fakturowni ponowienia będą pomijane.

## [HIGH] firebase/functions/src/routes/webhooks.js:163

**Rezerwacja faktury nigdy nie jest zwalniana przy błędzie Fakturowni — martwy catch, bo createStandardInvoice nie rzuca**

- Scenariusz: Klient płaci 199 zł netto, Stripe dostarcza checkout.session.completed. wystawFakture() woła faktury.zarezerwuj(session.id), co OD RAZU tworzy dokument invoices/{session.id}. Następnie createStandardInvoice() trafia na chwilową awarię Fakturowni (HTTP 500 albo timeout) — ale ta funkcja łapie każdy błąd wewnętrznie i zwraca {created:false, error}, NIGDY nie rzuca (invoice.js: wszystkie ścieżki return, także degraded {degraded:true}). Dlatego blok catch { await faktury.zwolnij(session.id) } w wystawFakture jest kodem martwym, a zwracana wartość .created nie jest sprawdzana. Rezerwacja invoices/{session.id} zostaje. obsluzCheckout kończy się normalnie, markDone domyka zdarzenie, Stripe dostaje 200 i nie ponawia. Klient nigdy nie dostaje faktury VAT. Każda ponowna dostawa webhooka trafia w faktury.zarezerwuj -> false -> 'Faktura już istnieje, pomijam', więc faktura jest bezpowrotnie stracona i wymaga ręcznego wystawienia. Ta sama pułapka dotyka KAŻDEGO płacącego klienta, gdy Fakturownia działa w trybie degradacji/niecskonfigurowana — ich klucze sesji zostają zatrute i włączenie Fakturowni później ich nie obejmie.
- Naprawa: W wystawFakture sprawdzaj wynik zamiast polegać na wyjątku: const wynik = await createStandardInvoice(...); if (!wynik?.created) { await faktury.zwolnij(session.id).catch(()=>{}); logger.error(...); }. Dodatkowo nie rezerwuj klucza sesji w trybie degradacji (features.invoicing === false), żeby nie spalać session.id zanim fakturowanie w ogóle działa.

## [HIGH] firebase/functions/src/routes/webhooks.js:153

**Nieudane wystawienie faktury trwale blokuje ponowienie (klient placi, brak faktury VAT)**

- Scenariusz: wystawFakture() woła faktury.zarezerwuj(session.id) (tworzy dokument invoices/{session.id}) PRZED createStandardInvoice(). Kluczowe: createStandardInvoice() (services/invoice.js:38-56) łapie WSZYSTKIE błędy Fakturowni wewnętrznie i zwraca {created:false, error} — NIGDY nie rzuca. Gdy Fakturownia ma przejściową awarię (HTTP 500, timeout, sieć), blok try w wystawFakture kończy się bez wyjątku, więc catch z faktury.zwolnij() NIE wykonuje się. Rezerwacja invoices/{session.id} zostaje na stałe. Zdarzenie Stripe jest markDone, więc ponowna dostawa trafia w stripeEvents.claim → duplikat 200 i handleEvent nie jest wołane ponownie. Efekt: opłacony klient B2B nigdy nie dostaje faktury VAT (obowiązek podatkowy w PL) i nie ma ŻADNEJ ścieżki naprawy — rezerwacja blokuje retry, a błąd jest tylko w logu. Deklarowany cel zwolnij() ("żeby ponowienie mogło wystawić fakturę naprawdę") jest martwym kodem, bo createStandardInvoice nie rzuca.
- Naprawa: wystawFakture musi zwalniać rezerwację na podstawie WYNIKU, nie wyjątku: `const wynik = await createStandardInvoice(...); if (!wynik?.created && !wynik?.degraded) await faktury.zwolnij(session.id).catch(()=>{});` — albo zmienić createStandardInvoice, by rzucał na realnej awarii (a nie na degraded). Rezerwację zostawić tylko przy created===true (i ewentualnie degraded).

## [HIGH] backend/src/routes/auth.js:154

**Usuwanie konta zwraca 404 w produkcji — brak DELETE /auth/me w backendzie, na który wskazuje apka**

- Scenariusz: Apka mobilna (mobile/src/config.js:19) uderza w Railway backend. AccountScreen.js:97 wywołuje api.deleteAccount → DELETE /auth/me (mobile/src/api/client.js:65). W firebase/functions/src/routes/auth.js:183 ta trasa istnieje (recursiveDelete + anulujSubskrypcje), ale w backend/src/routes/auth.js NIE MA żadnego router.delete('/me') — jest tylko get/patch/put/post. Żądanie trafia w notFoundHandler → 404, użytkownik widzi 'Nie udało się usunąć konta'. Skutki: (1) łamie RODO art. 17 i wymóg Google Play o działającym usuwaniu konta w apce (ryzyko odrzucenia/zawieszenia w Play), (2) subskrypcja Stripe NIGDY nie jest anulowana — logika anulowania powstała wyłącznie w drzewie Firebase, więc karta jest obciążana za konto, którego user nie może usunąć.
- Naprawa: Przenieść trasę DELETE /auth/me (usunięcie danych + anulujSubskrypcje) z firebase/functions/src/routes/auth.js do backend/src/routes/auth.js, dopóki apka mobilna wskazuje na Railway; albo przełączyć mobile/src/config.js na Firebase dopiero po pełnym parytecie tras.

## [HIGH] mobile/src/config.js:17

**Mobile app PROD nadal wskazuje Railway, nie Firebase — migracja nie jest przełączona po stronie klienta**

- Scenariusz: API_URL w PROD jest zahardkodowane na 'https://backend-production-a43e3.up.railway.app'. Cała migracja PrzetargAI na Firebase (firebase/functions/**) jest zbudowana i wdrażana, ale żaden ekran aplikacji jej nie używa — logowanie, /matches, /upgrade dalej idą na Railway. Skutki: (1) Firebase i Railway to dwa równoległe, ROZJECHANE backendy PrzetargAI (np. subscriptionStatus, obsługa async_payment, timeout AI różnią się między drzewami), a żywy ruch trafia do Railway, do którego część napraw nie dotarła; (2) w chwili gdy ktoś wyłączy trasy PrzetargAI na Railway zgodnie z założeniem 'backend = tylko Fitter', KAŻDY użytkownik aplikacji dostanie 404 na logowaniu, matchach i płatności. Klient nigdy nie został przepięty na Firebase.
- Naprawa: Przepiąć PROD API_URL na URL funkcji Firebase (api PrzetargAI) i zweryfikować że wszystkie wywołania mobile trafiają na Firebase przed odcięciem tras PrzetargAI na Railway.

## [HIGH] backend/src/jobs/scheduler.js:21

**Cykl tender-fetch + scoring AI biegnie RÓWNOLEGLE na Railway i Firebase, z osobnymi licznikami budżetu — podwójny, niekontrolowany koszt Anthropic**

- Scenariusz: Railway startScheduler() rejestruje 'tender-fetch' → runTenderFetch → generateMatchesForAllUsers → scoreTenderMatch (Anthropic). Firebase index.js dailyTenderFetch robi DOKŁADNIE to samo o 12:00. Oba drzewa używają tego samego ANTHROPIC_API_KEY, ale liczą wydatki w OSOBNYCH tabelach ai_usage (Railway=SQLite aiUsage.record, Firebase=Firestore). Bramka aiBudgetAllows/budgetStatus widzi tylko połowę wydatku, więc twardy limit $500 jest egzekwowany per-system: realny rachunek Anthropic może sięgnąć ~2× limitu (np. $1000) zanim którykolwiek system się zatrzyma. Dodatkowo te same przetargi BZP są pobierane i oceniane dwa razy dziennie. Backend na Railway obsługuje też Fitter tym samym kluczem — martwy/duplikowany scoring PrzetargAI zżera limit współdzielony z żywym Fitterem.
- Naprawa: Wyłączyć 'tender-fetch' z Railway scheduler (zostawić tylko backup + trasy Fittera) — cykl PrzetargAI ma odtąd żyć wyłącznie w Firebase dailyTenderFetch. Alternatywnie odciąć trasy/cron PrzetargAI z backendu po pełnym cut-over klienta.

## [HIGH] backend/src/jobs/scheduler.js:53

**Cron PrzetargAI na Railway nie wyłączony — podwójne wykonanie po deployu Firebase (duplikaty push + podwójny koszt AI)**

- Scenariusz: startScheduler() (index.js:6) rejestruje bezwarunkowo zadanie 'tender-fetch' na KAŻDYM starcie backendu Railway; runTenderFetch → generateMatchesForAllUsers → matching + push. Jednocześnie Firebase (firebase/functions/index.js:79) ma onSchedule dailyTenderFetch na tym samym slocie '0 12 * * *'. Nie ma żadnej flagi (typu PRZETARG_MIGRATED) odcinającej cron PrzetargAI na Railway. W dniu gdy Firebase functions zostaną wdrożone, o 12:00 ODPALĄ SIĘ OBA systemy: dwa pobrania BZP, dwa przebiegi matchingu i DWA zestawy powiadomień push do tych samych użytkowników. Dyżurny o 3 w nocy dostaje zgłoszenia 'dostaję każde dopasowanie dwa razy' i nie ma jak tego skojarzyć — dwa harmonogramy w dwóch chmurach, żaden log nie wskazuje na drugi. Diagnoza wymaga świadomości, że stary scheduler nadal żyje.
- Naprawa: Dodać flagę env (np. PRZETARG_MIGRATED=1) która pomija zadanie 'tender-fetch' w schedulerJobs() po przełączeniu PrzetargAI na Firebase; zostawić tylko 'backup'. Alternatywnie usunąć tender-fetch z Railway w momencie deployu Firebase.

## [HIGH] backend/src/routes/webhooks.js:50

**Dwa równoległe handlery Stripe (Railway + Firebase) bez koordynacji — ryzyko podwójnej faktury VAT lub braku aktywacji**

- Scenariusz: Backend na Railway wciąż w pełni obsługuje checkout.session.completed dla PrzetargAI (setTier + createStandardInvoice, linie 78-90), a mobile produkcyjny nadal celuje w Railway (mobile/src/config.js:19 → backend-production-a43e3). Firebase ma bliźniaczy handler. Fakturownia nie ma klucza idempotencji, a oba drzewa trzymają rejestr faktur w NIEZALEŻNYCH magazynach (backend: stripe_events w SQLite; Firebase: kolekcja invoices w Firestore) — brak dedupu między drzewami. Scenariusz: po wdrożeniu Firebase, jeśli w Stripe zostanie dodany endpoint webhooka Firebase, a stary endpoint Railway nie zostanie wyłączony, Stripe dostarczy checkout.session.completed do OBU endpointów → dwie aktywacje i DWIE faktury VAT za jedną płatność. Wariant odwrotny (jeden endpoint zostaje na Railway): checkout utworzony przez Firebase nie zostanie aktywowany przez Railway (inny user_id) → klient zapłacił, konto zostaje Free. Dyżurny dostaje 'mam dwie faktury' albo 'zapłaciłem i nic'.
- Naprawa: Przy cutover wyłączyć trasę /upgrade i handler PrzetargAI w backend/src/routes/webhooks.js (zostawić tylko gałęzie metadata.project==='fitter'*), i upewnić się że w Stripe endpoint Railway obsługuje wyłącznie zdarzenia Fittera (filtrowanie event types per endpoint).

## [HIGH] firebase/functions/test/indeksyFirestore.test.js:24

**Firestore-index guard only scans repos.js — the actual collectionGroup query lives in admin.js, so its test is vacuous**

- Scenariusz: The test reads ONLY src/db/repos.js (const REPOS = ...'/src/db/repos.js'). Its own docstring says its job is to catch queries that need a composite index because 'emulator nie egzekwuje indeksow zlozonych, wiec testy tego nie wykrywaly'. But the only collectionGroup query in the codebase is in src/routes/admin.js:103 (.collectionGroup('matches').orderBy('created_at','desc')). repos.js has zero collectionGroup usages, so the 'collectionGroup ma zadeklarowany indeks' test hits `if (!uzycia.length) return;` and asserts NOTHING. Concrete mutation that ships broken to prod with all tests + emulator E2E green: delete the matches COLLECTION_GROUP entry from firestore.indexes.json (or add any `.where('x','==',v).orderBy('y')` to matches.js/admin.js). Tests stay green; GET /admin/recent-matches then throws FAILED_PRECONDITION in production — exactly the crash class this test claims to prevent.
- Naprawa: Scan every source file that issues Firestore queries (src/routes/**, src/jobs/**, src/services/**), not just repos.js — e.g. read all *.js under src/ and run the where/orderBy and collectionGroup regexes over the concatenation; assert `uzycia.length > 0` so the collectionGroup branch can never pass vacuously.

## [HIGH] firebase/functions/test/stripeEvents.test.js:18

**stripeEvents.claim atomicity is never actually raced — test claims 'rownolegla dostawa' but issues sequential awaits**

- Scenariusz: The test message says 'rownolegla dostawa false' / 'obsluga w toku — duplikat odrzucony', but the two claims are awaited sequentially (line 20-21), never concurrently. A mutation that replaces `db().runTransaction(...)` in repos.js stripeEvents.claim (repos.js:670) with a plain `ref.get()` then `ref.set()` passes every test: the first claim writes {status:'processing'}, the sequential second claim reads it and returns false. But under the real threat — Stripe delivering the same event to two function instances at once (the exact 'co najmniej raz' redelivery the whole rejestr exists to defend) — both instances read 'not exists' and both write 'processing', both return true, and the customer gets a DOUBLE VAT invoice + double activation email. No test would catch that regression.
- Naprawa: Add a test that fires two claim() calls concurrently on a fresh event id via Promise.all and asserts exactly one true / one false, forcing the transaction to be real (emulator supports concurrent transactions).

## [HIGH] firebase/functions/src/services/matching.js:151

**The paid-AI scoring branch of the matching engine has ZERO test coverage — every test forces features.ai=false**

- Scenariusz: All 15 test files set process.env.ANTHROPIC_API_KEY='' and none mocks scoreTenderMatch, so features.ai is always false. That means matching.js lines 129-143 (the AI branch: in-loop aiQuota.reserve, scoreTenderMatch, score override, scorer='ai', aiOdpowiedzialo) never execute in any test, and every test asserts aiCalls===0. The core, most expensive, most business-critical path is untested. Concrete undetected regression: change line 151 `const ocenaOstateczna = aiOdpowiedzialo || !wartoPytacAi;` to `= true` — this re-introduces the exact documented audit bug (a candidate scored heuristically during an AI outage gets a permanent evaluations trace and NEVER returns to the model, silently poisoning feed quality forever). All tests still pass. Same for breaking the `score = ai.score`/`scorer='ai'` override.
- Naprawa: Add matching tests that inject a fake AI scorer (stub scoreTenderMatch via a module mock or a seam) so the AI-answered path runs: assert score/scorer are overwritten, that aiQuota.reserve is consumed, and that a heuristic-only evaluation during simulated AI failure does NOT leave a final trace (ocenaOstateczna=false).

## [HIGH] firebase/functions/src/services/matching.js:131

**Cała gałąź AI silnika dopasowań + całe services/ai.js nie mają ŻADNEGO testu (features.ai=false w każdym teście)**

- Scenariusz: W .env.test ANTHROPIC_API_KEY jest pusty, a każdy plik testowy dodatkowo robi process.env.ANTHROPIC_API_KEY=''. Skutkiem features.ai=false, więc w generateMatchesForUser warunek `wartoPytacAi = features.ai && ...` jest ZAWSZE false. Linie 108-143 (odczyt aiQuota.used, wyliczenie aiBudget = min(AI_RERANK_TOP_N, limitDobowy - zuzytoDzis), bramka `await aiQuota.reserve(user.id, limitDobowy)`, dekrementacja aiBudget, wywołanie scoreTenderMatch, ustalenie aiOdpowiedzialo i ocenaOstateczna dla ścieżki 'AI odpowiedziało') nigdy się nie wykonują. Cały services/ai.js (budgetStatus z cache, bramka hardExceeded/softExceeded, parseScore na śmieciach z modelu, alarmy budżetu) też nie jest importowany przez żaden test. Test 'HIGH (denial-of-wallet)' w matching.test.js:113 woła aiQuota.reserve BEZPOŚREDNIO, nie przez silnik — więc udaje, że broni wiring, którego nie dotyka. Mutacja: zamień `await aiQuota.reserve(user.id, limitDobowy)` na `true` (usuń bramkę denial-of-wallet) albo usuń bramkę `if (status.hardExceeded) return null` w ai.js — WSZYSTKIE testy nadal zielone, a produkcja traci ochronę portfela i twardy limit budżetu.
- Naprawa: Dodać test integracyjny silnika z wstrzykniętym atrapowym klientem AI (albo modułem mockującym scoreTenderMatch/budgetStatus), uruchamiany z niepustym ANTHROPIC_API_KEY w osobnym procesie: sprawdzić, że po wyczerpaniu aiQuota.reserve silnik przestaje wołać AI, że hardExceeded degraduje do heurystyki, oraz osobne testy jednostkowe parseScore (JSON w prozie, score=NaN, brak pola score, score>100).

## [HIGH] firebase/functions/src/routes/webhooks.js:68

**Orkiestracja webhooka POST /stripe nie ma testu integracyjnego — tylko prymitywy repo**

- Scenariusz: stripeEvents.test.js testuje wyłącznie repos (claim/markDone/release/faktury). Żaden test nie napędza router.post('/stripe') ani handleEvent/obsluzCheckout — a przy features.stripe=false w testach constructWebhookEvent i tak rzuca serviceUnavailable, więc trasy nie da się przetestować w obecnym setupie. Niesprawdzona jest KRYTYCZNA kolejność z komentarza: markDone jest w osobnym try (linie 68-73) właśnie po to, by błąd domknięcia NIE uruchamiał release() i nie powodował podwójnej obsługi (druga faktura, drugi e-mail). Mutacja: przenieś `await stripeEvents.markDone(event.id)` z powrotem do bloku try razem z handleEvent (linia 49) — przy błędzie markDone poleci release(), Stripe ponowi dostawę i klient dostanie DRUGĄ fakturę VAT. Wszystkie testy przechodzą. Tak samo niesprawdzone: bramka payment_status!=='paid' (aktywacja przed wpływem pieniędzy), pominięcie zdarzeń Fittera, obsługa braku usera.
- Naprawa: Test integracyjny z atrapą Stripe (mock constructWebhookEvent zwracający gotowe event) na PRAWDZIWYM serwerze express (jak w zdrowie.test.js): zdarzenie completed z payment_status='paid' aktywuje plan i rezerwuje fakturę raz; powtórna dostawa nie dubluje; symulowany błąd markDone NIE kasuje rezerwacji zdarzenia; payment_status!='paid' nie aktywuje.

## [HIGH] mobile/src/screens/MatchFeedScreen.js:79

**Nieudane odświeżenie (pull-to-refresh) lub ponowne wejście na ekran KASUJE całą wczytaną listę i pokazuje pełnoekranowy błąd**

- Scenariusz: Użytkownik ma wczytanych 20+ przetargów. Robi pull-to-refresh (albo wchodzi w szczegół i wraca — `useFocusEffect` w linii 59 też woła `load()`). W tym momencie sieć na chwilę pada (metro, winda). `load()` na catch ustawia `error` (linie 32-33), a render w liniach 79-92 przy niepustym `error` zwraca pełnoekranowy widok 'Nie udało się wczytać przetargów', wymazując poprawnie wyświetloną listę. To dokładne przeciwieństwo starannego `dociagnijWiecej`, które celowo NIE wywala listy. Skutek: przelotny błąd sieci niszczy widok, mimo że aplikacja miała ważne dane.
- Naprawa: Traktować błąd odświeżania/re-focus nie-destrukcyjnie: gdy `matches.length > 0`, zachować listę i pokazać błąd jako baner/toast (np. 'Nie udało się odświeżyć'), a pełnoekranowy `if (error)` uruchamiać tylko gdy nic jeszcze nie ma (`matches.length === 0`).

## [HIGH] mobile/src/screens/MatchFeedScreen.js:32

**Chwilowy blad sieci na focus/pull-refresh kasuje caly zaladowany feed**

- Scenariusz: Uzytkownik ma zaladowanych 20 dopasowan, wchodzi w szczegoly przetargu i wraca (back). useFocusEffect wola load() bez trybu; jesli w tym momencie sieć mrugnie (metro, tunel), catch ustawia setError(err.message). Render ma `if (error) return <ekran bledu>`, wiec cala widoczna lista znika i pojawia sie pelnoekranowe 'Nie udalo sie wczytac przetargow'. To samo przy pull-to-refresh, ktory sie nie powiedzie: onRefresh -> load('refresh') -> blad -> lista przepada. Sciezka dociagania (dociagnijWiecej) zostala celowo zabezpieczona przed tym (komentarz w kodzie 'Blad dociagania nie moze wywalic juz wyswietlonej listy'), ale focus i pull-refresh NIE — asymetria.
- Naprawa: W load() traktuj blad inaczej gdy lista juz istnieje: jesli matches.length > 0, nie przelaczaj na pelnoekranowy ekran bledu, tylko pokaz nieinwazyjny toast/baner ('Nie udalo sie odswiezyc') i zostaw stare dane. Pelnoekranowy error tylko dla pierwszego, pustego ladowania.

## [HIGH] mobile/src/screens/MatchFeedScreen.js:59

**Chwilowy błąd sieci przy powrocie na feed kasuje całą wczytaną listę i pokazuje pełnoekranowy błąd**

- Scenariusz: useFocusEffect wywołuje load() bez trybu przy KAŻDYM wejściu na ekran (linia 59). load() na wyjątku robi setError(err.message), a render ma twardy guard `if (error) return <ekran błędu>` (linia 79), który wygrywa nad wciąż wypełnionym stanem `matches`. Scenariusz: użytkownik ma wczytane 20 dopasowań, wchodzi w szczegóły przetargu i wraca (Back) — w tym momencie na 1s pada sieć (metro, winda). useFocusEffect refetchuje, dostaje bladSieci(), i cały feed znika, zastąpiony ekranem „Nie udało się wczytać przetargów”. Użytkownik traci widok, który miał sekundę temu. To niespójne z dociagnijWiecej (linia 51), które celowo zachowuje listę przy błędzie. Push otworzy powiadomienie -> ten sam efekt.
- Naprawa: Nie przełączaj na pełnoekranowy błąd, gdy `matches` nie jest puste. Pokaż błąd tylko przy pierwszym ładowaniu (loading==true wcześniej) lub zamień na nieinwazyjny toast/banner nad listą; przy odświeżeniu na focus zachowaj poprzednie dane tak jak dociagnijWiecej.

## [HIGH] store/listing.md:65

**Opis w App Store kieruje do zakupu subskrypcji przez stronę WWW — ryzyko odrzucenia (App Store 3.1.1 anti-steering)**

- Scenariusz: Opisy App Store ('Subskrypcję Standard aktywujesz wygodnie przez stronę internetową') i Google Play ('aktywujesz wygodnie przez stronę internetową przetargai.pl') jawnie kierują użytkownika do zakupu cyfrowej subskrypcji poza sklepem. Subskrypcja odblokowuje funkcje w apce (nielimitowane dopasowania, push). PrzetargAI nie jest 'reader app', więc nie ma wyjątku. Reviewer Apple standardowo odrzuca takie zgłoszenia wg wytycznej 3.1.1 (payments/anti-steering). Blokuje przejście review iOS.
- Naprawa: Albo (a) wdrożyć zakup przez Apple IAP / StoreKit dla iOS i usunąć wzmiankę o zakupie na WWW z metadanych iOS, albo (b) usunąć z opisu App Store wszelkie odesłania do zakupu subskrypcji na stronie (zostawić opis funkcji bez informacji o cenie/gdzie kupić) i sprawdzić, czy w apce nie ma linków do checkoutu WWW. Zweryfikuj też ekran 'upgrade' w apce — jeśli otwiera Stripe/WWW na iOS, to również narusza 3.1.1.

## [HIGH] codemagic.yaml:96

**Android versionCode utyka na 1 — Google Play odrzuci każdą aktualizację po pierwszym uploadzie**

- Scenariusz: Krok 'Numer wersji' robi `echo "versionCode=$(($BUILD_NUMBER + 100))" >> gradle.properties`, ale build.gradle wygenerowany przez `expo prebuild` ma zaszyte `versionCode 1` (app.json nie ma android.versionCode, więc domyślnie 1) i nie czyta tej właściwości z gradle.properties. Każdy AAB wychodzi z versionCode=1. Pierwszy upload na ścieżkę internal przejdzie, ale drugi build kończy się błędem Play Console 'Version code 1 has already been used'. iOS jest OK, bo używa agvtool (linia 57).
- Naprawa: Sparametryzować versionCode w build.gradle (np. `versionCode Integer.parseInt(project.findProperty('VERSION_CODE') ?: '1')`) i ustawiać VERSION_CODE, albo w skrypcie podmienić `versionCode 1` sed-em w android/app/build.gradle na $(($BUILD_NUMBER + 100)) — analogicznie do agvtool na iOS.

## [HIGH] mobile/app.json:36

**Brak extra.eas.projectId + build bez EAS = token push zawsze null → płatna funkcja push (plan Standard) martwa dla wszystkich**

- Scenariusz: services/push.js woła Notifications.getExpoPushTokenAsync(projectId ? {projectId} : undefined), a projectId czyta z Constants.expoConfig?.extra?.eas?.projectId. W app.json NIE MA sekcji extra.eas.projectId, nie ma eas.json, a build idzie przez Codemagic `expo prebuild` (nie EAS). W SDK 51+ getExpoPushTokenAsync bez projectId rzuca 'No projectId found' → catch → return null → token nigdy nie trafia na serwer. Backend (functions/src/services/push.js) wysyła wyłącznie przez Expo Push API i akceptuje tylko ExponentPushToken[...]. Efekt: nikt nie dostaje powiadomień push, mimo że to reklamowana wartość planu Standard (199 zł/mc). Płacący klient nie dostaje nic — i nikt tego nie zauważy, bo błąd jest cichy (console.warn).
- Naprawa: Dodaj do app.json extra.eas.projectId z UUID projektu EAS (i owner/slug), albo — skoro i tak jest google-services.json i FCM — przełącz się na getDevicePushTokenAsync() (token FCM) po stronie mobile ORAZ wysyłkę FCM po stronie backendu zamiast Expo Push API. Przetestuj rejestrację tokenu na realnym buildzie release, nie tylko w Expo Go.

## [HIGH] codemagic.yaml:96

**versionCode na sztywno 1 — Google Play przyjmie pierwszy AAB, ale odrzuci każdą kolejną aktualizację**

- Scenariusz: app.json android nie ma versionCode, więc `expo prebuild` generuje android/app/build.gradle z zahardkodowanym `versionCode 1`. Krok CI 'Numer wersji' robi `echo versionCode=... >> gradle.properties`, ale wygenerowany przez Expo build.gradle NIE czyta versionCode z gradle.properties (bierze literał z defaultConfig). Każdy build wychodzi z versionCode=1. Pierwszy upload na Play przejdzie, ale przy pierwszym hotfixie/aktualizacji Play Console odrzuci AAB: 'Version code 1 has already been used'. O 3 w nocy nie wypuścisz poprawki krytycznego buga — bloker nie premiery, ale całego cyklu utrzymania.
- Naprawa: Albo ustaw android.versionCode w app.json i podbijaj go per release, albo zmodyfikuj wygenerowany build.gradle w CI (sed/patch) tak, by versionCode czytał `project.hasProperty('versionCode') ? project.versionCode.toInteger() : 1`. Zweryfikuj versionCode w finalnym .aab (bundletool dump manifest), nie zakładaj że echo do gradle.properties zadziałało.

## [MEDIUM] firebase/functions/src/services/stripe.js:58

**anulujSubskrypcje nie obsluguje subskrypcji JUZ anulowanej → usuniecie konta (RODO art.17) trwale zablokowane**

- Scenariusz: anulujSubskrypcje traktuje jako 'już nie ma' WYŁĄCZNIE err.code==='resource_missing'. Subskrypcja o statusie 'canceled' NADAL istnieje w Stripe (jest pobieralna), ale ponowny stripe.subscriptions.cancel() zwraca błąd invalid_request_error (nie resource_missing) → rethrow. Scenariusz złego szczęścia: przy DELETE /me pierwszy cancel WYKONUJE się w Stripe, ale odpowiedź HTTP ginie (blip sieci) → SDK rzuca → routes/auth.js:193 łapie i zwraca 503 serviceUnavailable, konto NIE jest usuwane, a stripe_subscription_id pozostaje ustawione (czyszczone dopiero PO udanym cancelu, linia 210). Użytkownik ponawia DELETE → anulujSubskrypcje woła cancel na już-anulowanej (ale istniejącej) subskrypcji → błąd != resource_missing → rethrow → znowu 503. Konto staje się NIEUSUWALNE, mimo że komentarz funkcji obiecuje 'już anulowana to NIE błąd'. Kod nie realizuje własnego kontraktu i blokuje prawo do usunięcia danych.
- Naprawa: W catch potraktować jako sukces także status 'canceled': gdy err wskazuje już-anulowaną subskrypcję (np. err.code!=='resource_missing' ale subskrypcja jest canceled — pobrać ją przez stripe.subscriptions.retrieve i sprawdzić status==='canceled') zwrócić false zamiast rzucać. Alternatywnie: przed cancelem retrieve i pominąć cancel, jeśli status już 'canceled'.

## [MEDIUM] backend/src/routes/matches.js:13

**Dociąganie kolejnych stron feedu martwe — apka używa kursora (next_before), backend zwraca stronicowanie po offset**

- Scenariusz: MatchFeedScreen.js:31 czyta data.next_before i zapisuje jako kursor; dociagnijWiecej (linia 44-48) przerywa gdy !kursor i wysyła ?before=<kursor>. Firebase (firebase/functions/src/routes/matches.js:35) zwraca next_before i czyta ?before. Ale backend/src/routes/matches.js GET '/' liczy limit+offset i zwraca {matches,count,limit,offset} — bez pola next_before i ignorując parametr before. Ponieważ apka wskazuje na Railway, po pierwszej stronie kursor=null → 'Załaduj więcej' nigdy się nie odpala. User widzi tylko najnowsze ~20 dopasowań, a starsza historia staje się nieosiągalna (dokładnie regresja, którą komentarz w MatchFeedScreen.js:41-42 miał naprawić). Naprawa kursora trafiła tylko do drzewa Firebase.
- Naprawa: Ujednolicić kontrakt: dodać stronicowanie kursorem (before/next_before) do backend/src/routes/matches.js GET '/', lub przełączyć apkę na Firebase. Trasy PrzetargAI w obu drzewach muszą zwracać ten sam kształt odpowiedzi.

## [MEDIUM] backend/src/routes/webhooks.js:16

**Backend nadal aktywuje subskrypcje PrzetargAI do martwej bazy SQLite — ryzyko podwójnej faktury / rozjazdu stanu**

- Scenariusz: backend/src/routes/webhooks.js (Railway) wciąż obsługuje checkout.session.completed dla PrzetargAI: aktywuje usera i tier w SQLite oraz wystawia fakturę Fakturowni. Ten sam scenariusz obsługuje firebase/functions/src/routes/webhooks.js do Firestore. Jeśli po migracji w Stripe zarejestrowany jest tylko endpoint Railway (bo apka wskazuje na Railway), to aktywacja i faktura idą do bazy SQLite — spójne dopóki backend jest systemem żywym, ale każdy klient, który zaloguje się przez Firebase, nie zobaczy planu. Jeśli natomiast oba endpointy są aktywne w Stripe, to samo zdarzenie płatności trafia do DWÓCH niezależnych rejestrów idempotencji (stripe_events w SQLite vs Firestore) i wystawia DWIE faktury VAT za jedną płatność. Rejestr per-sesja chroni tylko wewnątrz jednego drzewa.
- Naprawa: Zdecydować który endpoint webhooka jest źródłem prawdy dla PrzetargAI i wyrejestrować drugi w Stripe; do czasu cutoveru trzymać aktywację PrzetargAI wyłącznie w drzewie, do którego wskazuje apka (obecnie Railway).

## [MEDIUM] backend/src/routes/webhooks.js:86

**Webhook Stripe PrzetargAI aktywny na Railway I na Firebase — ryzyko podwójnej faktury Fakturownia za jedną płatność**

- Scenariusz: Backend /webhooks/stripe dalej obsługuje checkout.session.completed dla PrzetargAI (session bez metadata.project) → users.setTier + createStandardInvoice (Fakturownia). Firebase webhooks.js robi to samo. Idempotencja stripeEvents.claim działa TYLKO w obrębie jednego systemu (SQLite vs Firestore) — nie chroni między systemami. Jeśli podczas migracji nie usunięto starego endpointu Railway ze Stripe (typowy błąd), Stripe dostarczy to samo zdarzenie do OBU URL: klient dostaje DWIE faktury z Fakturowni za jedną opłatę (problem księgowy/prawny) oraz dwa maile aktywacyjne. Ponieważ mobile płaci przez Railway /upgrade (client_reference_id → users SQLite), Firebase-owy webhook dodatkowo nie znajdzie usera w Firestore i wygeneruje niespójny stan.
- Naprawa: Upewnić się że w Stripe zarejestrowany jest tylko JEDEN aktywny endpoint webhooka PrzetargAI (docelowo Firebase) i usunąć drugi; po cut-over odciąć /webhooks PrzetargAI na Railway.

## [MEDIUM] backend/src/services/fitterScan.js:19

**Klient Anthropic Fittera (żywy na Railway) bez timeout/maxRetries — naprawa trafiła tylko do Firebase**

- Scenariusz: Firebase ai.js dostał `new Anthropic({ timeout: 30_000, maxRetries: 1 })`, ale kod Fittera na Railway (fitterScan.js, fitterAi.js oraz services/ai.js) tworzy klienta bez timeoutu — obowiązuje domyślny 10-minutowy timeout SDK. Skan ISO to duże żądanie z wizją; przy zawieszeniu po stronie Anthropic połączenie HTTP trzymane jest do 10 min. aiLimiter dopuszcza 6 żądań/min/IP — kilka równoległych zawieszonych skanów potrafi zająć wątki/połączenia backendu na kwadranse, degradując żywy produkt Fitter (i inne trasy Railway). Naprawa timeoutu istnieje tylko w martwym (nieużywanym przez klienta) drzewie Firebase.
- Naprawa: Dodać `{ timeout: 30_000, maxRetries: 1 }` do klientów Anthropic w backend/src/services/fitterScan.js, fitterAi.js i ai.js.

## [MEDIUM] backend/src/services/ai.js:7

**Wywołania AI Fittera na Railway bez timeoutu — poprawka 30s trafiła tylko do Firebase**

- Scenariusz: Poprawka timeout:30_000/maxRetries:1 na kliencie Anthropic została dodana WYŁĄCZNIE do firebase/functions/src/services/ai.js:16. W backendzie klient w ai.js:7 oraz — co ważniejsze dla żyjącego kodu Fittera — w fitterAi.js:24 i fitterScan.js:19 tworzony jest new Anthropic({apiKey}) BEZ timeoutu, więc obowiązuje domyślny SDK = 10 minut. Scan ISO to wywołanie wizji z fotem 3-8 MB; przy spowolnieniu/zawieszeniu upstream Anthropic pojedyncze żądanie /api/fitter/scan-iso trzyma połączenie na Railway do 10 minut bez żadnego logu błędu. O 3 w nocy objaw to wiszące żądania i wyczerpane połączenia bez wpisu w logach aż do timeoutu — bardzo trudne do zdiagnozowania.
- Naprawa: Skopiować konfigurację klienta z firebase ai.js (timeout:30_000, maxRetries:1) do backend/src/services/{ai,fitterAi,fitterScan}.js.

## [MEDIUM] backend/src/services/ai.js:34

**Zerwany inwariant wspólnego budżetu AI — po migracji dwa niezależne limity $500**

- Scenariusz: Komentarz bramki budżetu twierdzi 'PrzetargAI i Fitter dzielą jedną tabelę ai_usage, więc dzielą też limit'. Po migracji to NIEPRAWDA: backend liczy zużycie z SQLite (aiUsage.monthCostUsd), a Firebase z Firestore. Każda strona egzekwuje własny twardy limit ($500) niezależnie i nie widzi wydatków drugiej — łączny realny wydatek może sięgnąć ~2x zakładanego budżetu, a alarm przekroczenia (zglosPrzekroczenieBudzetu w firebase) odnosi się tylko do liczników Firestore. Dyżurny widzący rachunek Anthropic 2x wyższy od limitu nie znajdzie w żadnym pojedynczym liczniku wyjaśnienia, bo każdy pokazuje 'w limicie'.
- Naprawa: Po migracji zaktualizować komentarz/inwariant i albo obniżyć limity per-drzewo tak by suma = budżet docelowy, albo scentralizować licznik ai_usage (jedno źródło) czytany przez oba drzewa.

## [MEDIUM] firebase/functions/test/matching.test.js:113

**aiQuota denial-of-wallet test proves the cap sequentially but never the concurrency it exists to stop**

- Scenariusz: The 'denial-of-wallet' test loops `await aiQuota.reserve(...)` five times in series and asserts [true,true,true,false,false]. Replacing the `db().runTransaction` in repos.js aiQuota.reserve (repos.js:538) with a naive read-then-increment passes this test unchanged. The atomicity actually matters when the nightly cron (generateMatchesForAllUsers) and a concurrent PATCH /auth/me → backfillUser hit the same user's ai_quota doc at once: two parallel reads both see calls<limit and both write calls+1, letting a user exceed the per-day paid-AI cap and drain the shared monthly budget the cap defends. The sequential test can never observe that.
- Naprawa: Add a concurrent test: Promise.all of (limit+3) reserve() calls on one user and assert exactly `limit` returned true and aiQuota.used===limit.

## [MEDIUM] firebase/functions/test/matching.test.js:120

**Test aiQuota.reserve nie broni przed wyścigiem, który sam nazywa 'denial-of-wallet'**

- Scenariusz: Test o nazwie 'denial-of-wallet' woła aiQuota.reserve() 5 razy SEKWENCYJNIE i oczekuje [true,true,true,false,false]. Mutacja: zamiana runTransaction w src/db/repos.js:538 (aiQuota.reserve) na zwykły odczyt+zapis (const doc = await ref.get(); if(calls>=limit)...; await ref.set(...)) PRZECHODZI ten test bez zmian, bo brak współbieżności. A wyścig jest realny: PATCH /auth/me odpala backfillUser(updated) fire-and-forget (auth.js:148), który dla tego samego usera może biec RÓWNOLEGLE z dziennym cyklem (generateMatchesForUser nie jest chroniony backfillCooldown w cronie). Dwa równoległe reserve() odczytują ten sam calls=N, oba zapisują N+1 i oba zwracają true — dobowy limit AI (120) zostaje przekroczony, wspólny miesięczny budżet AI drenowany. To dokładnie ta klasa awarii, którą aiQuota miała zamknąć.
- Naprawa: Dodać test współbieżny: await Promise.all([...] .map(() => aiQuota.reserve(user.id, LIMIT))) na świeżym userze i asertować, że liczba true == LIMIT oraz aiQuota.used == LIMIT. To wymusi transakcyjność reserve().

## [MEDIUM] firebase/functions/src/routes/upgrade.js:30

**Cała trasa /upgrade (startCheckout) bez żadnego testu — naprawiony 'spalony token' może cicho wrócić**

- Scenariusz: Kluczowa naprawa audytowa w startCheckout to KOLEJNOŚĆ: peek (walidacja bez zużycia) -> createCheckoutSession -> consume (zużycie dopiero po sukcesie), żeby przejściowy błąd Stripe nie palił jednorazowego magic linka. Ta kolejność nie ma ŻADNEGO testu — grep w test/ nie znajduje upgrade.js ani consumeUpgradeLink. Same prymitywy magicLinks.consume są testowane (firestoreRepos), ale nie logika trasy. Mutacja: przeniesienie consumeUpgradeLink(userId, token) PRZED createCheckoutSession (regresja do stanu sprzed audytu) przechodzi wszystkie testy. Skutek w produkcji: każdy przejściowy błąd Stripe (sieć/brak price) zużywa token, magic link z maila jest martwy, użytkownik nie może wykupić Standard bez proszenia o nowy link -> utracona sprzedaż.
- Naprawa: Test integracyjny na startCheckout z zamockowanym createCheckoutSession rzucającym błąd: asertować, że magicLinks.peek(token,'upgrade') NADAL zwraca userId po nieudanej próbie (token nie zużyty), oraz że przy sukcesie token jest zużyty dokładnie raz.

## [MEDIUM] firebase/functions/src/services/ai.js:126

**Ścieżka AI (scoreTenderMatch, parseScore, bramka twardego budżetu) nie jest wykonywana w żadnym teście**

- Scenariusz: WSZYSTKIE pliki testowe ustawiają process.env.ANTHROPIC_API_KEY='' -> features.ai=false -> client=null, więc scoreTenderMatch zwraca null w 1. linii i nigdy nie wchodzi w budgetStatus/parseScore/aiUsage.record. To znaczy, że LENS-owe pytanie 'co gdy AI zwróci śmieci?' i 'co gdy budżet przekroczony?' nie ma pokrycia. Mutacje, które przechodzą cały npm test: (a) usunięcie clampu w parseScore (ai.js:113) — model zwracający score:150 zapisuje confidence_score:150 do dopasowania; (b) usunięcie bramki if(status.hardExceeded) return null (ai.js:130) — matching dalej płaci za AI po przekroczeniu twardego limitu $500, mimo że pricing.test.js pilnuje tylko liczenia kosztu, nie jego egzekwowania. Backend byłby ślepy na te regresje aż do rachunku.
- Naprawa: Test jednostkowy parseScore (eksportować ją) na wejściach: tekst bez JSON -> null, score:150 -> 100, score:'abc'/NaN -> null. Osobno test scoreTenderMatch z wstrzykniętym klientem-atrapą i wymuszonym hardExceeded=true (np. aiUsage.record kosztu > hard limit) asertujący, że zwraca null i NIE woła klienta.

## [MEDIUM] firebase/functions/test/indeksyFirestore.test.js:38

**Strażnik indeksów złożonych (naprawa CRITICAL) jest omijalny przez rozbicie where/orderBy na dwie instrukcje**

- Scenariusz: Test broni przed powtórką awarii, w której where('==')+orderBy(inne pole) wymaga indeksu złożonego, a emulator go nie egzekwuje -> produkcja martwa od 1. dnia. Guard opiera się na regexie wymagającym, by .where(...) i .orderBy(...) STYKAŁY SIĘ w jednym łańcuchu (\)\s*\.orderBy). Mutacja/nowy kod w repos.js typu: 'let q = col.where("deadline","==",null); q = q.orderBy("fetched_at","desc");' NIE jest dopasowywany przez regex -> test zielony, a zapytanie w produkcji rzuca FAILED_PRECONDITION i (przez Promise.all w openPool) wywraca cały silnik dopasowań. Dokładnie ta konstrukcja z rozbiciem na przypisania jest już używana w listForUser (repos.js:408-421), więc wzorzec jest realistyczny, nie hipotetyczny.
- Naprawa: Wzmocnić guard: albo scalić kod do postaci łańcuchowej i zabronić rozbijania, albo (lepiej) dodać test wykonawczy na emulatorze z załadowanymi indeksami przez firestore.rules/indexes, albo parsować AST zamiast regexa. Minimalnie: rozszerzyć regex o wariant 'zmienna = zmienna.orderBy(' i wykrywać orderBy po polu innym niż wcześniejszy where(==) na tej samej zmiennej.

## [MEDIUM] firebase/functions/src/routes/upgrade.js:52

**routes/upgrade.js i kolejność peek→Stripe→consume magic linka bez żadnego testu**

- Scenariusz: Cały routes/upgrade.js oraz consumeUpgradeLink (magicLink.js) nie są importowane przez żaden test. Naprawa audytowa z komentarza (linie 24-28): najpierw magicLinks.peek (walidacja bez zużycia), potem createCheckoutSession, i DOPIERO po sukcesie consumeUpgradeLink — żeby przejściowy błąd Stripe nie spalił jednorazowego tokenu. Mutacja: przywróć stare zachowanie (consume PRZED createCheckoutSession) albo zamień peek na consume w startCheckout — użytkownik, u którego Stripe chwilowo padnie, bezpowrotnie traci magic link i nie może ponowić upgrade'u. Żaden test tego nie łapie (magicLinks.consume jest testowany tylko jako prymityw w firestoreRepos.test.js, bez powiązania z trasą i z sesją Stripe).
- Naprawa: Test trasy z atrapą createCheckoutSession: gdy tworzenie sesji rzuca, magicLinks.peek nadal zwraca właściciela (link niezużyty) i ponowna próba działa; gdy sesja się powiedzie, drugie użycie tego samego tokenu jest odrzucone.

## [MEDIUM] firebase/functions/src/db/repos.js:279

**openPool porównuje surowy string deadline z BZP z kanonicznym nowIso() — near-boundary przetargi źle klasyfikowane**

- Scenariusz: normalizeNotice (bzp.js:64) zapisuje deadline DOSŁOWNIE tym, co zwróci BZP (firstOf submittingOffersDate...), bez normalizacji do ISO-Z; dateOnly jest stosowane tylko do parametrów wyszukiwania. openPool robi `where('deadline','>', nowIso())`, gdzie nowIso() = '2026-07-10T12:34:56.789Z'. Jeśli BZP zwraca deadline jako datę bez czasu ('2026-07-10') albo z offsetem strefy ('2026-07-10T14:00:00+02:00'), porównanie leksykograficzne myli się: (a) przetarg z terminem DZIŚ w formacie date-only ('2026-07-10' < '2026-07-10T12:...') wypada z puli jako 'po terminie', mimo że termin to koniec dnia; (b) '...+02:00' względem '...Z' porównuje się niespójnie (znak '+' vs 'Z', czas lokalny vs UTC), więc przetarg faktycznie już zamknięty bywa uznany za otwarty i odwrotnie. Klasa błędu jest niewidoczna dla testów, bo matching/firestoreRepos zawsze używają '2099-01-01T00:00:00.000Z'.
- Naprawa: Znormalizować deadline w normalizeNotice do kanonicznego new Date(x).toISOString() (albo odrzucać nieparsowalne), i dodać test openPool z deadline w formacie date-only oraz z offsetem strefy, sprawdzający poprawne wejście/wyjście z puli na granicy 'dziś'.

## [MEDIUM] firebase/functions/src/services/push.js:11

**services/push.js, invoice.js, email.js, routes/admin.js oraz feedback w routes/matches.js bez testów**

- Scenariusz: Żaden test nie importuje services/push.js. Filtr `t.startsWith('ExponentPushToken')` (linia 11) jest jedyną barierą chroniącą przed wysyłką do przypadkowych/nieprawidłowych tokenów do Expo Push API. Mutacja: usuń filtr `.filter(t => ... startsWith('ExponentPushToken'))` — sendPush zacznie POST-ować dowolne stringi (np. stary token FCM, pusty string) do exp.host; żaden test nie zgłasza regresji. Podobnie bez pokrycia: createStandardInvoice (kwota brutto 199*1.23, degradacja gdy brak Fakturowni), sendEmail, cały routes/admin.js (rzutowanie pól usera w /admin/users chroniące przed wyciekiem password_hash/stripe_customer_id — mutacja zwracająca `...u` przechodzi) oraz ścieżka feedback w routes/matches.js.
- Naprawa: Testy jednostkowe: sendPush odrzuca nie-Expo tokeny i zwraca {sent:0} bez wywołania fetch (mock globalnego fetch); admin /users nie zawiera password_hash/stripe_customer_id (test na kształcie odpowiedzi); createStandardInvoice liczy poprawne total_price_gross i degraduje bez rzucania gdy features.invoicing=false.

## [MEDIUM] mobile/src/screens/MatchFeedScreen.js:125

**Nowy użytkownik po rejestracji widzi 'Brak dopasowanych przetargów' — komunikat sugeruje błąd konfiguracji, a nie mówi, że dopasowania policzą się później**

- Scenariusz: Dopasowania generuje wyłącznie dobowy cykl (`dailyTenderFetch` → `generateMatchesForUser`), NIE rejestracja. Użytkownik, który założył konto tuż po ostatnim przebiegu crona, przez ~24h widzi pusty feed z komunikatem (linie 124-130): '...Sprawdź w zakładce Konto, czy profil ma ustawione słowa kluczowe.' Nawet gdy słowa kluczowe są poprawne, tekst oskarża o złą konfigurację i nie ustawia oczekiwania 'przetargi pojawią się wkrótce / do jutra'. Świeży user uzna, że apka nie działa, i odejdzie.
- Naprawa: Rozróżnić dwa stany pustego feedu: (a) brak słów kluczowych → prowadź do profilu; (b) profil OK, brak dopasowań → 'Szukamy pasujących przetargów. Nowe ogłoszenia sprawdzamy codziennie — pierwsze dopasowania zobaczysz wkrótce.' Stan zależny od `user.keywords/cpv_codes`.

## [MEDIUM] mobile/src/screens/MatchFeedScreen.js:59

**Każde wejście na ekran resetuje paginację do pierwszej strony — dociągnięta historia i pozycja przewijania przepadają**

- Scenariusz: Użytkownik przewija feed i przez `onEndReached` dociąga kilka stron (np. 80 pozycji). Wchodzi w szczegół przetargu i wraca. `useFocusEffect` (linia 59) woła `load()`, które przez `setMatches(data.matches || [])` (linia 30) podmienia listę na samą pierwszą 20-elementową stronę i cofa scroll na górę. Wcześniej obejrzane starsze dopasowania stają się znów nieosiągalne bez ponownego przewijania, a użytkownik traci miejsce, w którym był.
- Naprawa: Nie przeładowywać całej listy przy każdym focusie. Odświeżać tylko przy pierwszym montowaniu lub pull-to-refresh; ewentualnie scalać nowe pozycje z górą listy zamiast zastępować całość (zachowując kursor i offset przewijania).

## [MEDIUM] mobile/src/components/Button.js:18

**Brak accessibilityRole/Label na przyciskach i kartach — czytnik ekranu nie rozpoznaje kontrolek**

- Scenariusz: Uzytkownik VoiceOver/TalkBack (m.in. wymagane w audytach dostepnosci i przez recenzje App Store) otwiera ekran szczegolow. Przyciski to Pressable bez accessibilityRole='button'. '👍 Trafne' czytane jest jako 'thumbs up trafne' bez informacji, ze to przycisk; naglowek 'Konto' (Pressable w headerRight, MatchFeedScreen:64) i cala MatchCard tez nie sa oznaczone jako klikalne. Uzytkownik nie wie, co jest interaktywne.
- Naprawa: Dodaj accessibilityRole='button' i accessibilityLabel do Button (Pressable), do naglowka 'Konto', do MatchCard (accessibilityRole='button', label z tytulem+score), oraz zamien emoji-etykiety feedbacku na czytelne labelki ('Ocen jako trafne'). accessibilityState={{disabled: isDisabled, busy: loading}} na Button.

## [MEDIUM] mobile/src/context/AuthContext.js:57

**Prosba o zgode na push tuz po rejestracji — dla konta Free, ktore pushy nigdy nie dostanie**

- Scenariusz: persistSession() wola registerForPushNotifications() natychmiast po rejestracji/logowaniu, wiec systemowy dialog uprawnien push wyskakuje zanim uzytkownik zobaczyl jakakolwiek wartosc aplikacji (zly moment -> wysoki odsetek odmow, a na iOS raz odmowione trzeba wlaczac recznie w Ustawieniach). Co gorsza, matching.js:173 wysyla push WYLACZNIE gdy premium_tier==='standard', wiec konto Free oddaje token i zgode, ktorych apka i tak nie uzyje.
- Naprawa: Prosic o push dopiero w kontekscie wartosci (np. przy wlaczaniu powiadomien po wykupieniu Standard albo z ekranu Konto na wyrazna akcje), a dla Free w ogole nie inicjowac requestPermissions. Ewentualnie poprzedzic systemowy dialog wlasnym pre-promptem tlumaczacym korzysc.

## [MEDIUM] mobile/src/screens/MatchFeedScreen.js:122

**Pusty feed po rejestracji obwinia brak slow kluczowych, ktore user wlasnie podal**

- Scenariusz: Nowy uzytkownik konczy rejestracje (podal slowa kluczowe w formularzu) i widzi pusty feed, bo backfill nie znalazl pasujacych OTWARTYCH przetargow lub nowe ogloszenia dociagane sa dopiero nocnym cyklem. Komunikat mowi 'Sprawdz w zakladce Konto, czy profil ma ustawione slowa kluczowe' — kieruje go do naprawiania czegos, co wlasnie ustawil, i nie wspomina, ze dopasowania moga pojawic sie z kolejnym dobowym przetwarzaniem. User mysli, ze apka nie dziala.
- Naprawa: Rozroznic dwa stany: (a) profil ma slowa kluczowe -> komunikat 'Szukamy dla Ciebie przetargow. Nowe ogloszenia sprawdzamy codziennie — dopasowania pojawia sie tutaj, damy znac.' (b) profil pusty -> dopiero wtedy prosba o dodanie slow kluczowych. Stan (a) wynika z user.keywords dostepnego w AuthContext.

## [MEDIUM] mobile/src/screens/MatchFeedScreen.js:122

**Świeżo zarejestrowany użytkownik widzi pusty feed z mylącą radą, bez informacji że dopasowania są generowane**

- Scenariusz: Rejestracja uruchamia backfillUser w tle (auth.js:88), a feed ładuje się natychmiast przez useFocusEffect. Zanim onboarding matching skończy, ListEmptyComponent pokazuje „Brak dopasowanych przetargów… Sprawdź w zakładce Konto, czy profil ma ustawione słowa kluczowe”. Użytkownik, który przed chwilą WPISAŁ słowa kluczowe w rejestracji, dostaje sugestię że ich nie ma — brzmi to jak awaria. Nie ma auto-odświeżania po zakończeniu backfillu ani komunikatu „generujemy dopasowania, wrócą po najbliższym skanie (do 24h)”. Efekt: nowy płatny lead myśli, że apka nie działa, i odchodzi.
- Naprawa: Rozróżnić dwa stany pustej listy: (a) profil ma słowa kluczowe -> „Analizujemy przetargi pod Twój profil. Pierwsze dopasowania pojawią się w ciągu kilku minut, a nowe ogłoszenia dochodzą codziennie — pociągnij, aby odświeżyć.”; (b) brak słów kluczowych -> obecna rada. Rozważyć krótki auto-poll/odświeżenie tuż po rejestracji.

## [MEDIUM] mobile/src/components/Button.js:19

**Brak accessibilityRole/Label na Button, MatchCard i przyciskach nagłówka — czytnik ekranu nie ogłasza przycisków ani stanu ładowania**

- Scenariusz: Button to Pressable bez accessibilityRole="button" (Button.js:19), więc TalkBack/VoiceOver czyta tylko tekst, nie ogłasza że to przycisk. Gdy loading=true renderowany jest sam ActivityIndicator bez accessibilityLabel — niewidomy użytkownik po tapnięciu „Załóż konto” nie słyszy żadnego potwierdzenia, że akcja trwa. MatchCard (MatchCard.js:26) to Pressable bez accessibilityRole/Label; ScoreBadge dzieli wynik na dwa węzły Text („85” + „% dopasowania”), więc czytnik czyta je rozłącznie zamiast „dopasowanie 85 procent”. Pressable „Konto” w nagłówku (MatchFeedScreen.js:64) też bez roli/etykiety.
- Naprawa: Dodać accessibilityRole="button" + accessibilityState={{disabled,busy:loading}} do Button; accessibilityRole="button" i accessibilityLabel podsumowujący (tytuł + zamawiający + wynik + termin) do MatchCard; accessibilityLabel do ScoreBadge; accessibilityRole/Label do przycisku „Konto”.

## [MEDIUM] codemagic.yaml:96

**versionCode Androida nie jest realnie inkrementowany — powtórne wgranie do Play odrzucone jako duplikat**

- Scenariusz: Workflow android-release robi `expo prebuild` (regeneruje android/ z app.json, gdzie NIE ma android.versionCode → Expo ustawia domyślnie versionCode 1 na sztywno w app/build.gradle), a potem `echo "versionCode=$(($BUILD_NUMBER+100))" >> gradle.properties`. Wygenerowany przez Expo build.gradle ma literał `versionCode 1` i NIE czyta property `versionCode` z gradle.properties, więc dopisana wartość jest ignorowana. Każdy build AAB ma versionCode 1. Pierwsze wgranie przejdzie, ale każde kolejne (poprawka po odrzuceniu, aktualizacja) Google Play odrzuci: 'Version code 1 has already been used'.
- Naprawa: Ustaw android.versionCode w app.json i podbijaj przy releasie, LUB w scripts/configure-android-release-signing.mjs podmień w build.gradle literał `versionCode 1` na odczyt z property (np. versionCode Integer.parseInt(PRZETARGAI_VERSION_CODE)) i wstrzykuj go z CI, LUB użyj expo config plugin. Zweryfikuj po prebuild, że wygenerowany build.gradle faktycznie bierze versionCode z CI.

## [MEDIUM] store/asc-submission-pl.md:236

**Instrukcja zgłoszenia do App Store odsyła recenzenta i health-check do martwego backendu Railway**

- Scenariusz: Notatki dla recenzenta (linia 236: 'served by the backend at https://backend-production-a43e3.up.railway.app/polityka-prywatnosci') oraz sekcja 'Demo Account — health check' (linie 330-335) każą weryfikować logowanie demo curl-em na Railway /auth/login. Po migracji ta trasa dla przetargai nie istnieje → health-check zawsze pokaże 401/404 i wprowadzi w błąd ('konto trzeba odtworzyć'), a demo-konto trzeba tak naprawdę założyć w Firestore (Firebase), nie na Railway. Dokument zgłoszeniowy jest niespójny z faktycznym produktem.
- Naprawa: Podmienić wszystkie odwołania do backend-production-a43e3.up.railway.app na URL Functions (europe-central2-przetargai.cloudfunctions.net/api) i utworzyć konta apple-review@przetargai.demo / play-review@przetargai.demo w produkcyjnym Firestore z gotowymi dopasowaniami.

## [MEDIUM] codemagic.yaml:88

**CI (Codemagic) nie uruchamia żadnych testów — regresja przechodzi prosto do TestFlight/Play**

- Scenariusz: Oba workflowy (ios-release, android-release) robią tylko npm ci + prebuild + build; brak kroku `npm test` dla mobile i zero CI dla firebase/functions (brak .github/workflows). Mimo że repo ma pełne zestawy testów (functions z wymuszonym emulatorem, mobile), żaden nie biegnie przed publikacją. Scenariusz: ktoś łamie logikę paginacji/auth w mobile lub w Functions, commituje, CI zielone (bo tylko kompiluje), build ląduje na kanale internal/TestFlight z defektem u recenzenta.
- Naprawa: Dodać krok testów przed buildem: w mobile-workflowach `cd mobile && npm test` oraz osobny workflow uruchamiający testy firebase/functions na emulatorze (jak w npm test funkcji). Blokować build przy niepowodzeniu.

## [MEDIUM] firebase/functions/src/config.js:20

**APP_URL to tylko default (nie sekret, brak prod .env) — linki aktywacyjne i redirecty Stripe mogą prowadzić w 404 i nie da się tego poprawić bez redeployu**

- Scenariusz: APP_URL nie jest zadeklarowany jako defineSecret w index.js ani nie ma commitowanego functions/.env.<projekt> (są tylko .env.local i .env.test — emulator/testy, nieładowane w produkcji). W produkcji APP_URL = zahardkodowany default 'https://europe-central2-przetargai.cloudfunctions.net/api'. Ten URL jest wstawiany do successUrl/cancelUrl Stripe (upgrade.js:47-48) i do linku w mailu magic-link (magicLink.js:17). Dla funkcji gen2 (onRequest v2) realny publiczny URL to zwykle *.run.app; brak też rewrite'u Hosting (firebase.json nie ma sekcji hosting). Jeśli default nie zgadza się z faktycznym URL wdrożenia, klient po kliknięciu 'Aktywuj Standard' w mailu i po powrocie ze Stripe trafia w 404 — płatność nie domyka się, a naprawa wymaga redeployu (nie da się podmienić w runtime).
- Naprawa: Przed launchem ustaw APP_URL na realny URL wdrożonej funkcji przez commitowany functions/.env.przetargai (firebase functions v2 ładuje .env.<projectId> przy deployu) albo dodaj Firebase Hosting z rewrite na stabilną domenę (api.przetargai.pl) i wskaż ją w APP_URL. Zweryfikuj, że link z maila i redirect Stripe faktycznie otwierają działający endpoint.

## [LOW] firebase/functions/src/routes/webhooks.js:132

**E-mail aktywacyjny nie jest idempotentny (w przeciwieństwie do faktury) — dubel przy ponownej dostawie po nieudanym markDone**

- Scenariusz: obsluzCheckout wystawia fakturę przez faktury.zarezerwuj(session.id) (dedup per sesja), ale sendEmail(subscriptionActiveEmail) nie ma żadnej analogicznej ochrony. Komentarz przy markDone (linie 59-66) uzasadnia 'mniejsze zło' twierdząc, że 'faktura ma własną rezerwację per sesja, więc dubla nie wystawi' — pomija milcząco, że e-mail dubla wystawi. Scenariusz: markDone rzuca (chwilowa awaria Firestore), zwracamy 200, ale odpowiedź nie dociera do Stripe (timeout sieci) -> Stripe ponawia dostawę -> po 5 min rezerwacja zdarzenia wygasa -> claim() przejmuje ponownie -> obsluzCheckout leci drugi raz -> setTier standard (idempotentne) + DRUGI e-mail 'Subskrypcja aktywna', a faktura pominięta. Klient dostaje zdublowane powiadomienie.
- Naprawa: Objąć e-mail aktywacyjny tą samą rezerwacją per sesja co fakturę (albo osobny klucz 'email:<session.id>'), tak by ponowna obsługa tej samej sesji nie wysyłała drugiego maila.

## [LOW] mobile/src/screens/MatchDetailScreen.js:28

**Błędna polska odmiana przy najważniejszym polu (termin): 'za 1 dni', 'za 0 dni'**

- Scenariusz: W liniach 27-28 `deadlineText` skleja `` `za ${days} dni` ``. Dla terminu jutro (days=1) wyświetla 'za 1 dni' (poprawnie: 'za 1 dzień'), a dla terminu dziś (days=0) 'za 0 dni' zamiast 'dziś / ostatni dzień'. To najbardziej krytyczny czasowo element ekranu (deadline składania oferty), więc niepoprawna, myląca odmiana obniża zaufanie.
- Naprawa: Dodać poprawną odmianę: days===0 → 'dziś'; days===1 → 'za 1 dzień'; dla pozostałych reguła PL (2-4 → 'dni', 5+ → 'dni', ale 22/23/24 → 'dni'…) — użyć helpera odmiany albo prostszego 'termin za N dni' z warunkiem na 1 i 0.

## [LOW] mobile/src/components/Button.js:19

**Kontrolki interaktywne bez etykiet dostępności (accessibilityRole/Label) — czytnik ekranu nie nazywa przycisków, kart ani pól**

- Scenariusz: `Button` (Pressable, linia 19), `MatchCard` (Pressable) i pola `TextField` nie mają `accessibilityRole`/`accessibilityLabel`. Dla użytkownika VoiceOver/TalkBack przycisk w stanie `loading` (tylko spinner, brak tekstu) jest ogłaszany bez nazwy, karta przetargu nie ma roli 'button', a `TextInput` nie jest powiązany z etykietą (linia 9 w TextField renderuje label jako osobny Text). Osoba niewidoma nie wie, co kliknąć ani co wpisać.
- Naprawa: Dodać `accessibilityRole='button'` + `accessibilityLabel={title}` do Button (stały label mimo stanu loading), `accessibilityRole='button'` + opis (tytuł + % dopasowania) do MatchCard, oraz `accessibilityLabel={label}` do TextInput w TextField.

## [LOW] mobile/src/api/errors.js:29

**Fallback 'Blad serwera (500).' nic nie mowi uzytkownikowi co zrobic**

- Scenariusz: Gdy serwer zwroci 500/502 bez tresci (data.error.message brak), uzytkownik na ekranie bledu feedu lub w Alert widzi suche 'Blad serwera (500).'. Nie wie, czy to jego wina, czy ma czekac, ponowic, czy pisac do supportu — a w polaczeniu z findingiem #1 ten sam komunikat zastepuje mu cala liste.
- Naprawa: Dla statusow 5xx bez tresci dawac konkretny, uspokajajacy komunikat po polsku: 'Chwilowy problem po naszej stronie. Sprobuj ponownie za chwile — jesli sie powtarza, napisz na <support>.' Zostawic surowy '(500)' tylko jako drobny szczegol techniczny, nie jako glowna tresc.

## [LOW] mobile/src/screens/MatchDetailScreen.js:81

**Feedback nie jest odczytywany z serwera — po ponownym wejściu w oceniony przetarg znów widać przyciski oceny; oba przyciski migają spinnerem naraz**

- Scenariusz: Stan oceny jest lokalny (useState(null), linia 22) i nie pochodzi z serwera. Backend zapisuje ocenę (feedback.upsert), ale publicMatch nie zwraca jej, a ekran jej nie czyta. Scenariusz: użytkownik ocenia dopasowanie „👍 Trafne”, wychodzi, wraca w ten sam przetarg — znów pyta „Czy to dopasowanie było trafne?”, jakby nigdy nie ocenił; może ocenić drugi raz sprzecznie. Dodatkowo oba przyciski dostają loading={sending} (linie 93 i 100), więc po tapnięciu jednego spinner pojawia się na OBU — nie widać, którą opcję wybrano.
- Naprawa: Zwracać zapisaną ocenę w publicMatch i inicjalizować `feedback` z match; pokazywać spinner tylko na klikniętym przycisku (osobny stan „pending up/down”).

## [LOW] mobile/src/api/errors.js:29

**Komunikaty błędów typu „Błąd serwera (500).” trafiają dosłownie do Alertów i nic nie mówią użytkownikowi**

- Scenariusz: bladOdpowiedzi (errors.js:29) dla odpowiedzi bez data.error.message zwraca „Błąd serwera (500).”. Ten tekst jest pokazywany wprost: Alert.alert('Błąd', err.message) przy zapisie profilu (AccountScreen.js:36), upgrade (linia 73) i usuwaniu konta (linia 101), oraz jako treść ekranu błędu feedu. Scenariusz: backend zwraca 500/502 bez ciała (np. cold start, timeout Firestore) — użytkownik dostaje „Błąd serwera (500).”, nie wie czy to jego wina, czy ma spróbować później, czy stracił zapłatę przy upgrade.
- Naprawa: Dla 5xx podmieniać na komunikat akcyjny, np. „Serwer chwilowo niedostępny. Spróbuj ponownie za chwilę.”; dla błędu upgrade dodać wskazówkę „Jeśli obciążono kartę, plan aktywuje się automatycznie — sprawdź e-mail.”

## [LOW] store/listing.md:107

**Cennik Standard niespójny między dokumentami sklepowymi (199 zł vs 199 zł + VAT) — ryzyko Metadata Rejected**

- Scenariusz: listing.md (linia 107 i 155 opisu Google Play) podaje 'Standard (199 zł/mc)' bez VAT, natomiast store/asc-submission-pl.md (linia 155 opisu i 188 What's New) podaje '199 zł/mc + VAT'. Ta sama tabela odrzuceń w asc-submission (linia 320) sama ostrzega 'zostaw 199 PLN/mc spójnie wszędzie'. Rozbieżność ceny w metadanych między platformami/dokumentami to typowy powód odrzucenia metadanych i mylący komunikat dla klienta B2B (netto vs brutto).
- Naprawa: Ujednolicić brzmienie ceny we wszystkich polach obu sklepów (zdecydować: '199 zł/mc netto (+VAT)') i zsynchronizować z faktyczną kwotą Stripe price STRIPE_PRICE_STANDARD.

## [LOW] store/listing.md:57

**Listing reklamuje 'wartość' przetargu, której BZP nigdy nie zwraca — wprowadzenie w błąd i puste pole dla płacących**

- Scenariusz: Opis w store (App Store/Play) wymienia w szczegółach ogłoszenia 'zamawiający, termin, wartość, kod CPV'. Zgodnie z [[reference_bzp_api_dane]] ContractNotice z BZP praktycznie nigdy nie zawiera wartości zamówienia (0/500). MatchDetailScreen.js:63 pokazuje 'Szacowana wartość' tylko gdy budget jest obecny (`{budget ? <Row.../> : null}`), więc realnie użytkownik NIGDY nie zobaczy reklamowanego pola. To niezgodność treści listingu z produktem (ryzyko odrzucenia metadanych przez Apple i rozczarowanie płacącego klienta).
- Naprawa: Usuń 'wartość' z wypunktowania funkcji w store/listing.md i store/asc-submission-pl.md albo doprecyzuj 'wartość (gdy podana w ogłoszeniu)'. Trzymaj listing zgodny z realnym zestawem pól z BZP.

