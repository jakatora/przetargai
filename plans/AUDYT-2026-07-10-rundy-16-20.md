# Audyt PrzetargAI — rundy 16–20 (2026-07-10)

## [HIGH] firebase/functions/src/db/repos.js:660

**TTL magic_links nie dziala — expires_at to string ISO, a nie Timestamp; linki nigdy nie wygasaja z bazy**

- Scenariusz: firestore.indexes.json deklaruje fieldOverride TTL na magic_links.expires_at (skladnia poprawna). Ale magicLinks.create() zapisuje expires_at jako `new Date(...).toISOString()` — czyli STRING, tak samo nowIso(). Firestore TTL dziala WYLACZNIE na polach typu Timestamp; dokumenty, w ktorych pole TTL nie jest znacznikiem czasu, sa przez politykę IGNOROWANE i nigdy nie kasowane. Skutek: kazdy wygenerowany magic link (upgrade checkout) zostaje w kolekcji magic_links NA ZAWSZE, z user_id w srodku. Przy aktywnym korzystaniu kolekcja rosnie w nieskonczonosc, wbrew zalozeniu retencji, ktore mial egzekwowac TTL. Czyszczone sa tylko przy usunieciu konta (query po user_id), nie po wygasnieciu.
- Naprawa: Zapisywac expires_at jako Firestore Timestamp (Timestamp.fromDate(new Date(...))) zamiast toISOString(), albo dodac zadanie czyszczace przeterminowane linki. Uwaga: consume()/peek() porownuja expires_at < nowIso() leksykograficznie — po zmianie na Timestamp trzeba dostosowac te porownania.

## [HIGH] firebase/functions/src/db/repos.js:660

**Deklarowany TTL magic_links jest martwy — expires_at zapisywany jako string ISO, nie Timestamp**

- Scenariusz: firestore.indexes.json deklaruje fieldOverride TTL: {collectionGroup:'magic_links', fieldPath:'expires_at', ttl:true}. Ale magicLinks.create() zapisuje expires_at jako `new Date(...).toISOString()` (repos.js:660) oraz consume/peek porownuja `expires_at < nowIso()` — czyli pole jest STRINGIEM. Firestore TTL usuwa dokument WYLACZNIE gdy pole TTL jest typu Timestamp; dla stringa (albo braku pola) polityka nie robi nic. Skutek: kolekcja magic_links rosnie w nieskonczonosc i nigdy nie jest czyszczona automatycznie (poza recznym kasowaniem przy usuwaniu konta). Deklarowana kontrola retencji (i argument 'link wygasa po 10 min => znika') jest cichym no-opem — o 3 w nocy nikt tego nie zauwazy, bo walidacja `expires_at < nowIso()` dziala poprawnie leksykograficznie i linki i tak sa odrzucane po czasie; rosnie tylko baza. Dodatkowo: gdyby TTL kiedys zaczal dzialac po zmianie na Timestamp, porownania stringowe by sie wywalily.
- Naprawa: Zapisywac expires_at jako Firestore Timestamp (Timestamp.fromDate(new Date(...))), a porownania w peek/consume oprzec na Timestamp/Date zamiast leksykografii stringow. Wtedy zadeklarowany TTL zacznie realnie kasowac wygasle linki.

## [HIGH] firebase/functions/src/routes/auth.js:183

**Subskrybent Standard nie moze usunac konta (RODO art. 17 / wymog Google Play) — brak jakiejkolwiek anulacji subskrypcji w apce**

- Scenariusz: DELETE /auth/me blokuje usuniecie gdy premium_tier==='standard' (auth.js:183) z komunikatem 'Najpierw anuluj subskrypcje'. W AccountScreen.js przycisk 'Usun konto' jest disabled dla isStandard (linia 195), a jedyna wskazowka to tekst 'Najpierw anuluj subskrypcje Standard'. Jednoczesnie NIE ISTNIEJE zaden endpoint ani UI do anulowania subskrypcji: brak Stripe Billing Portal, brak POST /cancel, api/client.js nie ma metody anulacji, a webhooki tylko REAGUJA na anulacje zrobiona gdzie indziej. Scenariusz: placacy uzytkownik chce skorzystac z prawa do usuniecia danych — nie moze, bo w apce nie ma zadnej sciezki do anulowania subskrypcji, a usuniecie jest za nia zablokowane. To lamie RODO art. 17 dla platnikow i wprost narusza wymog Google Play o usuwaniu konta z poziomu apki (ten wymog jest wrecz cytowany w komentarzu przy usunKonto).
- Naprawa: Dodac endpoint anulacji subskrypcji (Stripe subscriptions.update cancel_at_period_end albo link do Billing Portal) i przycisk w AccountScreen, albo pozwolic usunac konto od razu anulujac subskrypcje po stronie Stripe w tym samym request.

## [HIGH] firebase/functions/src/services/bzp.js:64

**deadline z BZP zapisywany dosłownie i porównywany leksykograficznie z nowIso() — założenie 'UTC z Z' jest niesprawdzone**

- Scenariusz: normalizeNotice() bierze submittingOffersDate wprost z BZP (bez konwersji na kanoniczne ISO-UTC), a openPool (repos.js:279) filtruje `where('deadline','>', nowIso())`, gdzie nowIso() = '2026-07-10T20:00:00.000Z'. Firestore porównuje STRINGI. (1) Gdy BZP zwraca czas lokalny z offsetem, np. '2026-07-10T21:00:00+02:00' (czyli 19:00Z, termin JUŻ MINĄŁ o 20:00Z), string sortuje się WIĘKSZY niż now (znak '1'>'0' na pozycji godziny) → wygasły przetarg zostaje w puli i trafia do userów jako aktywny. (2) Gorzej: jeśli BZP kiedykolwiek zwróci format inny niż '...Z' — datę samą '2026-07-10' (prefiks now → sortuje się MNIEJ → przetarg z terminem DZIŚ, ostatni dzień na ofertę, znika z puli na cały dzień) albo format PL 'DD.MM.YYYY' (zawsze < '2026...' → CAŁA pula pusta, zero dopasowań dla wszystkich, bez błędu w logach). Runbook nie precyzuje formatu ani strefy, testy używają tylko '2099-...Z'.
- Naprawa: W normalizeNotice znormalizować deadline do kanonicznego UTC ISO: `const d=new Date(raw...); deadline = Number.isNaN(d)?null:d.toISOString()`. Wtedy porównanie leksykograficzne z nowIso() jest poprawne niezależnie od formatu wejściowego BZP; przy nieparsowalnej dacie → null (trafia do gałęzi bezTerminu, nie zabija całej puli).

## [HIGH] firebase/functions/src/services/matching.js:67

**Broad-profile users permanently starved beyond CANDIDATE_HEAD=120: pula jest cięta do top-120 PRZED odsianiem już ocenionych**

- Scenariusz: generateMatchesForUser ranks the pool and does `.sort(desc).slice(0, CANDIDATE_HEAD)` (linie 63-67) BEFORE computing `juzOcenione`/`swiezi` (linie 73-75). Heurystyka jest deterministyczna, więc top-120 to co dzień ten sam zbiór. Scenariusz: użytkownik standard z szerokim profilem (np. CPV '45' = cała budowlanka) ma w puli 2000 np. 400 przetargów ze score ≥ progu. Dzień 1: wszystkie 120 (dailyBudget=Infinity dla standard) dostają wpis w `evaluations`. Dzień 2+: `idsAmong` zwraca całe 120, `swiezi` jest puste, pętla nie robi nic. Przetargi z rankingu 121-400 (wszystkie trafne!) NIGDY nie są oceniane ani dopasowywane — dopóki któryś z top-120 nie zniknie z puli przez wygaśnięcie terminu. Przetargi z `deadline == null` lub odległym terminem nie wygasają nigdy → trwałe zagłodzenie. Użytkownik po cichu traci setki trafnych przetargów, bez błędu w logu.
- Naprawa: Odsiej już ocenione PRZED cięciem: najpierw `ranked = pool.map(score).filter(>0).sort(desc)`, potem pobierz `juzOcenione` dla szerszego zbioru (lub całości), następnie `swiezi = ranked.filter(nie oceniony).slice(0, CANDIDATE_HEAD)`. Wtedy każdy przebieg bierze kolejne świeże top-N, a nie wciąż te same 120 zajęte przez już ocenione.

## [HIGH] firebase/functions/src/jobs/fetchTenders.js:15

**Jeden fetch 500 na 7-dniowe okno gubi wiekszosc ogloszen BZP**

- Scenariusz: runTenderFetch woła searchNotices({page:0, size:500}), a searchNotices ustawia PublicationDateFrom = dzis - BZP_LOOKBACK_DAYS (domyslnie 7 dni) i PublicationDateTo = dzis. BZP publikuje setki ContractNotice DZIENNIE, wiec w 7-dniowym oknie jest realnie 1500-2800 ogloszen. Skoro PageNumber jest zepsuty (komentarz w pliku), pojedyncza strona 500 zwraca tylko ~500 z nich (najstarsze albo najnowsze, zaleznie od sortowania API), a reszta jest bezpowrotnie tracona. Konkretny scenariusz: cron nie zadzialal przez 1 dzien (deploy / awaria BZP -> funkcja aboruje caly przebieg) — nastepnego dnia jeden fetch 500 nie jest w stanie nadrobic luki, bo okno 7 dni ma >500 pozycji; ogloszenia z pominietego dnia poza pierwsza 500-ka nie trafiaja nigdy do bazy, a wiec nigdy nie generuja dopasowan. Uzytkownik z waskim profilem po prostu nie widzi swojego przetargu.
- Naprawa: Skoro parametry dat DZIALAJA (to udokumentowany filtr), petluj po JEDNYM dniu: dla kazdego z ostatnich BZP_LOOKBACK_DAYS dni wywolaj searchNotices z publishedFrom==publishedTo==tenDzien i size=500. Dziennie wolumen rzadko przekracza 500, wiec omijasz zepsuty PageNumber przez waskie okna dat. Retry/backoff juz jest, wiec 7 malych zadan miesci sie w budzecie.

## [HIGH] firebase/functions/src/services/ai.js:52

**Alarm o TWARDYM limicie budzetu AI zaglusza sie alarmem miekkim (wspolny throttle) — degradacja do heurystyki niewidoczna**

- Scenariusz: zglosPrzekroczenieBudzetu() dzieli JEDEN znacznik ostatniAlarmBudzetu (linia 48) i JEDEN odstep 1h dla obu rodzajow. Scenariusz w cronie o 12:00 w intensywnym dniu: wydatek przekracza limit MIEKKI -> pada alarm 'miekki', ostatniAlarmBudzetu = now. W tym samym przebiegu (kilka-kilkanascie sekund pozniej) wydatek przekracza limit TWARDY -> scoreTenderMatch woła zglosPrzekroczenieBudzetu(status,'twardy'), ale Date.now()-ostatniAlarmBudzetu < 1h => return na linii 52, alarm KRYTYCZNY ('Oceny AI WYLACZONE — matching na samej heurystyce') nigdy nie trafia do Error Reporting. Po przekroczeniu twardego limitu scoreTenderMatch zwraca null zanim naliczy koszt, wiec kolejnych prob AI (a wiec i kolejnej szansy na alarm) tego dnia juz nie ma. Dyzurny nie dostaje sygnalu, ze caly matching zdegradowal do heurystyki — dowiaduje sie dopiero z reklamacji userow o gorszych dopasowaniach.
- Naprawa: Rozdzielic throttle na osobne znaczniki per rodzaj (ostatniAlarm.miekki / ostatniAlarm.twardy) albo pozwolic 'twardy' zawsze przebic throttle ustawiony przez 'miekki' (twardy to inne, wazniejsze zdarzenie). Alarm o wylaczeniu AI musi wyjsc niezaleznie od wczesniejszego ostrzezenia miekkiego.

## [HIGH] firebase/functions/src/routes/admin.js:21

**Ręczny /admin/fetch-tenders ginie po 60 s — główne narzędzie diagnostyczne operatora nie działa na produkcyjnych danych**

- Scenariusz: POST /admin/fetch-tenders wywołuje runTenderFetch() SYNCHRONICZNIE w handlerze funkcji `api`. W index.js `export const api = onRequest({ secrets, memory: '512MiB' }, ...)` NIE ustawia timeoutSeconds, więc obowiązuje domyślny limit gen2 = 60 s. Tymczasem sam cykl ma wewnętrzny budżet 440 s (BUDŻET_CZASU_CYKLU_MS) i zakłada limit 540 s, jak scheduled `dailyTenderFetch` (który jawnie ma timeoutSeconds:540). Na realnych danych: 500 sekwencyjnych tenders.upsert() + generateMatchesForAllUsers po wszystkich userach spokojnie przekracza 60 s → platforma zabija żądanie w połowie, cykl kończy się częściowy, a operator dostaje timeout/502. To narzędzie odpalane jest dokładnie wtedy, gdy ktoś zgłasza 'nie dostaję dopasowań' — i wtedy właśnie zawodzi. dailyTenderFetch działa, bo ma 540 s; ta sama praca przez `api` — nie.
- Naprawa: Nie uruchamiać pełnego cyklu w request-response funkcji `api`. Albo ustawić `api` timeoutSeconds większy i zsynchronizować z budżetem, albo (lepiej) niech /admin/fetch-tenders enqueue'uje zadanie / wywołuje pubsub, a nie liczy cyklu w HTTP. Minimum: dopasować timeoutSeconds funkcji `api` do wewnętrznego budżetu 440 s.

## [HIGH] firebase/functions/src/routes/webhooks.js:99

**Faktura VAT wystawiana tylko przy pierwszym checkoucie — odnowienia subskrypcji nigdy nie dostają faktury**

- Scenariusz: Faktura powstaje wyłącznie w obsluzCheckout, wołanym dla checkout.session.completed / async_payment_succeeded. handleEvent NIE obsługuje invoice.payment_succeeded ani invoice.paid. W miesiącu 2., 3., ... Stripe automatycznie obciąża kartę i wysyła invoice.payment_succeeded (+ customer.subscription.updated ze statusem 'active', gdzie user.premium_tier==='standard' → wczesny return w obsluzZmianeSubskrypcji, bez akcji). createStandardInvoice nie zostaje wywołane, więc za każdy kolejny opłacony okres subskrybent jest obciążany bez faktury VAT (obowiązek ustawowy w PL), mimo że strona /upgrade/success obiecuje 'Fakturę dostaniesz e-mailem'. Brak jakiegokolwiek śladu/alertu, że faktura jest należna, a nie wystawiona.
- Naprawa: Dodać w handleEvent case 'invoice.payment_succeeded' (dla billing_reason==='subscription_cycle'), który dobiera usera po customerze i woła createStandardInvoice; pominąć pierwszą fakturę (subscription_create), żeby nie dublować z checkoutem.

## [HIGH] firebase/functions/src/routes/webhooks.js:145

**Nieudane wystawienie faktury/e-maila jest połykane, a zdarzenie oznaczane jako obsłużone — brak retry/dead-letter/alertu**

- Scenariusz: W obsluzCheckout tier jest ustawiany na 'standard', a następnie sendEmail(...).catch(log) i createStandardInvoice(...).catch(log) są fire-and-forget. Gdy Fakturownia zwróci 5xx/timeout albo features.invoicing jest false (brak sekretów FAKTUROWNIA_*), createStandardInvoice zwraca {created:false} i tylko loguje. Webhook przechodzi dalej do stripeEvents.markDone(event.id) → zdarzenie zamknięte na stałe, Stripe nigdy nie ponowi. Efekt: klient zapłacił, ma plan Standard, ale faktura VAT nigdy nie powstała i nie ma żadnej kolejki/flagi do reconcyliacji ani alertu (tylko linijka w logu). To samo dotyczy niewysłanego e-maila aktywacyjnego.
- Naprawa: Zapisywać nieudaną fakturę do kolekcji 'pending_invoices' (dead-letter) i emitować zdarzenie ReportedErrorEvent, albo NIE oznaczać markDone gdy faktura się nie powiodła i pozwolić Stripe ponowić (operacje idempotentne dzięki stripe_events).

## [HIGH] firebase/functions/package.json:10

**Domyslny `npm test` pomija emulator - ~45 testow integracyjnych cicho SKIP, a suita jest zielona**

- Scenariusz: Skrypt `test` to `node --env-file=.env.test --test test/*.test.js`, a .env.test NIE ustawia FIRESTORE_EMULATOR_HOST. Testy warstwy Firestore bramkuja sie `const skip = process.env.FIRESTORE_EMULATOR_HOST ? false : 'wymaga emulatora'`: firestoreRepos.test.js (14), matching.test.js (15), stripeEvents idempotencja (6), usuniecieKonta.test.js (5), zdrowie.test.js (5). Node przy { skip } konczy kodem 0. Brak CI (.github/workflows nie istnieje) wymuszajacego test:emu. Scenariusz dyzurnego o 3:00: hotfix do db/repos.js, uruchamia naturalne `npm test`, widzi all-green, wdraza - a regresja repo/matchingu/idempotencji webhooka/atomowosci usuniecia konta NIE zostala wykonana ani razu. Realnie biegna tylko testy czystych funkcji (scoring/cpv/textNorm/pricing/clientKey/nip).
- Naprawa: Uczyn emulator wymogiem: gdy brak FIRESTORE_EMULATOR_HOST test ma FAILowac, nie skipowac; albo domyslny `test` ma startowac emulator jak `test:emu`. Dodaj CI wolajace `npm run test:emu`.

## [HIGH] firebase/functions/test/stripeEvents.test.js:76

**Testy uprawnien do planu sprawdzaja KOPIE zbioru, nie realny kod webhooka**

- Scenariusz: Linie 76-77 deklaruja lokalna kopie reguly: `const UPRAWNIA = new Set(['active','trialing','past_due']); const tier = s => UPRAWNIA.has(s)?'standard':'free'`, po czym testy (79, 88) asertuja TE kopie. Nie importuja `UPRAWNIA_DO_STANDARD` ani `obsluzZmianeSubskrypcji` z routes/webhooks.js. To jedyne testy tieru z skip:false (biegna bez emulatora), wiec daja zludzenie pokrycia. Scenariusz: ktos edytuje webhooks.js:19 i usuwa 'trialing' lub 'past_due' -> klienci w okresie probnym / past_due leca do planu 'free' (utrata oplaconego dostepu) -> oba testy DALEJ ZIELONE, bo testuja kopie, nie kod produkcyjny.
- Naprawa: Wyekstrahuj UPRAWNIA_DO_STANDARD do importowalnej funkcji (jak backend/lib/subscriptionStatus.js) i testuj REALNY symbol zaimportowany z webhooks.js, nie lokalna atrape.

## [HIGH] firebase/functions/test/stripeEvents.test.js:76

**Test regul planu duplikuje tabele decyzyjna zamiast importowac ja z webhooks.js (test tautologiczny)**

- Scenariusz: Testy 'klient traci Standard' i 'past_due zachowuje dostep' (linie 79-93) sprawdzaja lokalna kopie `const UPRAWNIA = new Set(['active','trialing','past_due'])` zdefiniowana w samym tescie (komentarz: 'Odtworzenie tabeli decyzyjnej z routes/webhooks.js'), a NIE produkcyjne `UPRAWNIA_DO_STANDARD` z webhooks.js. To jedyny test reguly 'kto ma platny plan', dziala pod zwyklym `npm test` (skip:false) i daje falszywe poczucie pokrycia. Scenariusz: ktos edytuje webhooks.js:19 i przez pomylke dopisuje 'unpaid' (albo usuwa 'past_due') do UPRAWNIA_DO_STANDARD -> klient bez oplaty zachowuje Standard w nieskonczonosc / placacy traci dostep, a caly zestaw testow dalej jest zielony, bo test asertuje wlasna kopie. webhooks.js nie eksportuje tej staluej, wiec test i tak nie moglby jej zaimportowac.
- Naprawa: Wydzielic funkcje tier z webhooks.js do osobnego modulu (jak backend/src/lib/subscriptionStatus.js -> tierPrzetargAi), wyeksportowac ja i importowac w tescie zamiast kopiowac zbior.

## [HIGH] firebase/functions/package.json:10

**Domyslne `npm test` po cichu POMIJA wszystkie testy integracyjne Firestore (silnik dopasowan, usuniecie konta, idempotencja Stripe, health)**

- Scenariusz: 5 plikow (matching, firestoreRepos, usuniecieKonta, zdrowie, stripeEvents) bramkuje niemal kazdy test przez `{ skip: NA_EMULATORZE ? false : 'wymaga emulatora' }`. `npm test` (skrypt bez emulatora) uruchamia tylko kilka czystych asercji, a caly rdzen — generateMatchesForUser (budzety, kwoty AI, cooldown, atomowy zapis matches+evaluations), atomowosc usunKonto, claim/markDone/release Stripe, 503 martwego crona — jest raportowany jako SKIPPED, exit 0, zielono. Pokrycie daja dopiero `npm run test:emu`, a codemagic.yaml buduje wylacznie apke mobilna i NIE odpala zadnych testow funkcji w CI. Scenariusz: deweloper/CI robi `npm test`, widzi sukces, merguje regresje w silniku dopasowan lub w usuwaniu konta, ktora nigdy nie zostala wykonana.
- Naprawa: Ustawic domyslny skrypt `test` na `test:emu` (emulators:exec) lub oznaczac brak emulatora jako blad w CI zamiast cichego skip; dodac krok testow funkcji do pipeline CI.

## [HIGH] firebase/functions/test/stripeEvents.test.js:76

**Test uprawnien do planu sprawdza LOKALNA kopie reguly, nie kod webhooka**

- Scenariusz: stripeEvents.test.js linie 76-94 definiuja WLASNY `const UPRAWNIA = new Set(['active','trialing','past_due'])` i `const tier = (status)=>...` wewnatrz pliku testowego i asertuja na TEJ kopii ('klient ktory przestal placic TRACI plan', 'past_due zachowuje dostep'). Prawdziwa logika zyje w routes/webhooks.js (UPRAWNIA_DO_STANDARD, obsluzZmianeSubskrypcji, regula 'cancel_at_period_end to zapowiedz nie fakt'). Test jest tautologia: jesli ktos zmieni webhooks.js tak, ze past_due -> free, albo ze cancel_at_period_end natychmiast obniza plan, albo w ogole usunie obsluzZmianeSubskrypcji, WSZYSTKIE te testy dalej sa zielone. Cala sciezka pieniedzy (obsluzCheckout: aktywacja tieru, straznik payment_status!=='paid', wystawienie faktury; obsluzUsuniecieSubskrypcji; filtr zdarzen Fittera) nie ma ANI JEDNEGO testu dotykajacego realnego handlera — stripeEvents.test.js poza tym testuje tylko claim/markDone/release.
- Naprawa: Zaimportowac i testowac realny kod: wystawic UPRAWNIA_DO_STANDARD/mapowanie z webhooks.js jako eksport i asertowac na nim, albo (lepiej) wolac handleEvent/obsluz* z zamockowanym repo users i sprawdzac faktyczne setTier. Usunac lokalna atrape reguly.

## [MEDIUM] firebase/functions/src/db/repos.js:366

**Kursor startAfter(created_at) gubi dopasowania o identycznym znaczniku czasu na granicy strony**

- Scenariusz: Uzytkownik ma dopasowania, gdzie na pozycjach 20 i 21 (przy limit=20) created_at jest IDENTYCZNY co do milisekundy (nowIso() = rozdzielczosc ms, a cykl dzienny tworzy dopasowania seriami w tej samej chwili). Strona 1 zwraca 20 wierszy, ostatni ma created_at=X, wiec matches.js:31 ustawia next_before=X. Strona 2 wola listForUser z przed=X -> q.startAfter(X) na orderBy('created_at','desc') zwraca WYLACZNIE dokumenty o created_at < X (startAfter jest wykluczajacy dla samej wartosci). Dopasowanie z pozycji 21 (rowniez created_at=X) zostaje pominiete i NIGDY nie pojawi sie w feedzie — cicha utrata dopasowanego przetargu u placacego klienta.
- Naprawa: Nie uzywac skalarnej wartosci jako kursora. Paginowac przez DocumentSnapshot (pobrac ostatni snap i q.startAfter(lastDoc)) albo dodac stabilny tie-breaker: orderBy('created_at','desc').orderBy('__name__','desc') i przekazywac w kursorze pare (created_at, docId).

## [MEDIUM] firebase/functions/src/db/repos.js:366

**Kursor feedu po nieunikatowym created_at gubi dopasowanie na granicy strony**

- Scenariusz: listForUser paginuje przez orderBy('created_at','desc').startAfter(przed), gdzie przed = created_at ostatniego wiersza poprzedniej strony (routes/matches.js:31, next_before). created_at to nowIso() z rozdzielczoscia milisekundowa. Gdy DWA dopasowania jednego usera maja identyczny created_at (realne: dwa trafienia heurystyczne CPV zapisywane batchem w tym samym cyklu/tiku zegara, albo zgrubszy timer) i wypadaja dokladnie na granicy strony (np. wiersz 20 i 21 przy limit=20), to: strona 1 zwraca 20 wierszy, next_before = T (created_at wiersza 20). Strona 2 robi startAfter(T), a startAfter WYKLUCZA dokumenty rowne kursorowi — 21. dopasowanie o created_at == T zostaje trwale pominiete i nigdy nie pojawia sie w feedzie. Uzytkownik bezpowrotnie traci dopasowany przetarg z historii (mobile dociagnijWiecej po prostu go nie doda). Symetrycznie startAfter na wartosci rownej moze tez zdublowac klucz w FlatList (keyExtractor=item.id).
- Naprawa: Dodaj stabilny tiebreaker do zapytania i do kursora: orderBy('created_at','desc').orderBy(FieldPath.documentId(),'desc'), a next_before nios oba (created_at + id) lub uzyj kursora z DocumentSnapshot (startAfter(lastDocSnap)) zamiast surowej wartosci created_at.

## [MEDIUM] firebase/functions/src/routes/webhooks.js:76

**markDone-failure path can double-bill: reservation expires and a redelivered event re-runs the non-idempotent invoice/email**

- Scenariusz: checkout.session.completed: handleEvent fully succeeds (setTier + createStandardInvoice + sendEmail run), then stripeEvents.markDone(event.id) fails (transient Firestore error in the narrow window after handleEvent). The handler swallows it and returns 200, so the stripe_events doc stays status='processing' with claimed_at=now. It expires after REZERWACJA_WYGASA_PO_MS (5 min). If Stripe ever delivers that same event id again (Stripe does emit occasional duplicates independent of our 200), claim() sees age>5min and re-processes the whole event: obsluzCheckout runs createStandardInvoice a SECOND time (Fakturownia has no idempotency key here) and sends a second activation email. The comment claims ops are 'w większości idempotentne', but invoicing and email are not. Result: customer gets two invoices for one payment. Diagnosable only by correlating the 'nie udało się go domknąć' log with a later duplicate-processing log.
- Naprawa: Make markDone best-effort-but-also make re-processing safe: give createStandardInvoice an idempotency key derived from session.id (Fakturownia dedup) and/or record a per-session 'invoiced' marker checked before invoicing, so a re-processed event cannot mint a second invoice/email.

## [MEDIUM] firebase/functions/src/db/repos.js:167

**Usuniecie konta (RODO art. 17) nie propaguje do Stripe — email i dane rozliczeniowe zostaja u procesora**

- Scenariusz: Uzytkownik wykupil Standard (utworzony Customer w Stripe z jego emailem, stripe_customer_id zapisany), pozniej anulowal subskrypcje (tier wraca do 'free' przez webhook customer.subscription.deleted), a nastepnie usuwa konto. usunKonto() kasuje dokument users/{id} z subkolekcjami, unique/* i magic_links, ale NIE wola Stripe o usuniecie/anonimizacje Customera. Obiekt Customer (email, historia platnosci) zostaje w Stripe bezterminowo. Polityka prywatnosci obiecuje usuniecie danych i wymienia Stripe jako odbiorce — erasure powinno objac procesora. To dziura w sledzeniu danych end-to-end: dane 'usunietego' uzytkownika dalej istnieja poza Firestore.
- Naprawa: W usunKonto (lub w kroku poprzedzajacym) wywolac Stripe API delete/anonimizacje Customera gdy uzytkownik ma stripe_customer_id, albo udokumentowac w polityce, ze dane rozliczeniowe u Stripe pozostaja na podstawie obowiazku podatkowego przez okreslony czas.

## [MEDIUM] firebase/functions/src/db/repos.js:566

**audit_logs bez TTL ani limitu retencji — IP + user_id rosna w nieskonczonosc i przezywaja usuniecie konta**

- Scenariusz: auditLogs.record() zapisuje user_id, action, ip_address, created_at (string) do kolekcji audit_logs przy kazdej akcji (register, login, login_failed, view_profile, update_profile, delete_account, zdarzenia subskrypcji). firestore.indexes.json deklaruje TTL wylacznie dla magic_links — audit_logs nie ma zadnego TTL ani zadania czyszczacego. usunKonto() celowo ich NIE kasuje. Skutek: adres IP powiazany z user_id (dane osobowe) jest przechowywany bezterminowo, takze po realizacji RODO art. 17, bez okreslonego okresu retencji — sprzeczne z zasada ograniczenia przechowywania (art. 5 ust. 1 lit. e) i z polityka prywatnosci, ktora nie podaje okresu przechowywania dziennika audytu. Dodatkowo, gdyby probowano dodac TTL na created_at, tez by nie zadzialal (string, nie Timestamp — jak w magic_links).
- Naprawa: Ustalic i wdrozyc okres retencji audit_logs (np. 12 mies.) przez TTL na polu typu Timestamp lub zaplanowane zadanie czyszczace; opisac ten okres w polityce prywatnosci.

## [MEDIUM] firebase/functions/src/db/repos.js:566

**audit_logs rosnie bez konca i przechowuje adresy IP bez zadnej retencji/TTL**

- Scenariusz: auditLogs.record() zapisuje do globalnej kolekcji audit_logs pola user_id, action, detail oraz ip_address (repos.js:566). audit() jest wolany na niemal kazdym zadaniu: view_profile przy KAZDYM GET /auth/me, list_matches przy KAZDYM zaladowaniu feedu, view_match, login, login_failed, register itd. — czyli setki wpisow z IP na aktywnego uzytkownika dziennie. W firestore.indexes.json TTL zadeklarowano TYLKO dla magic_links; audit_logs nie ma ani TTL, ani zadnego jobu czyszczacego (grep: brak). usunKonto celowo ich NIE kasuje. Skutek: kolekcja adresow IP (dane osobowe) rosnie w nieskonczonosc, narusza zasade ograniczenia przechowywania (RODO art. 5 ust. 1 lit. e), a polityka prywatnosci §6 nie podaje okresu przechowywania danych technicznych/dziennika audytu. Z perspektywy dyzurnego: rachunek Firestore i rozmiar kolekcji rosna liniowo bez sufitu.
- Naprawa: Dodac fieldOverride TTL dla audit_logs.created_at (zapisujac created_at jako Timestamp) albo scheduled job kasujacy wpisy starsze niz ustalony okres retencji, i dopisac ten okres do polityki prywatnosci §6.

## [MEDIUM] firebase/functions/src/db/repos.js:662

**Deklarowany TTL na magic_links nie działa — pole expires_at to string, a Firestore TTL usuwa tylko pola typu Timestamp**

- Scenariusz: firestore.indexes.json deklaruje fieldOverrides dla magic_links.expires_at z ttl:true, ale magicLinks.create zapisuje expires_at jako ISO string (new Date(...).toISOString()), nie jako Firestore Timestamp. Polityka TTL ignoruje dokumenty, w których pole docelowe nie jest typu timestamp — nie usuwa ich nigdy. Skutek: każdy /auth/upgrade-link tworzy dokument magic_links; wygasłe/zużyte tokeny (user_id + token) NIGDY nie są usuwane automatycznie (jedyne czyszczenie to usunKonto przy kasowaniu konta). Kolekcja rośnie bez ograniczeń, a zadeklarowany mechanizm retencji jest martwy. Walidacja expiry działa nadal (peek/consume porównuje stringi), więc bezpieczeństwo jest OK, ale retencja/koszty nie. Naprawa: zapisywać expires_at jako Timestamp (Firestore Timestamp.fromDate) albo dodać zaplanowane czyszczenie.
- Naprawa: Zapisz expires_at jako Firestore Timestamp (np. Timestamp.fromDate(new Date(...))) zamiast ISO stringa, żeby polityka TTL faktycznie kasowała wygasłe magic_links; alternatywnie dodaj scheduled cleanup.

## [MEDIUM] firebase/firestore.indexes.json:11

**audit_logs rosną bez ograniczeń i przechowują adresy IP w nieskończoność — brak TTL i brak usuwania przy kasowaniu konta (RODO)**

- Scenariusz: auditLogs.record (repos.js:565) zapisuje user_id + ip_address + created_at do kolekcji audit_logs przy każdym register/login/login_failed/view_profile/list_matches/delete_account itd. Brak jakiegokolwiek TTL (fieldOverrides w firestore.indexes.json obejmują tylko magic_links) ani zaplanowanego czyszczenia. usunKonto CELOWO nie usuwa audit_logs, więc po realizacji prawa do usunięcia (art. 17) system nadal trzyma adresy IP i user_id byłego użytkownika bez zdefiniowanego okresu retencji ani limitu. Kolekcja rośnie w nieskończoność, a przechowywanie IP (dane osobowe) bez określonego terminu jest niezgodne z zasadą ograniczenia przechowywania. Polityka prywatnosci nie deklaruje okresu retencji logów audytu.
- Naprawa: Ustaw TTL na audit_logs.created_at (pole typu Timestamp) z konkretnym okresem (np. 12-24 mies.) i udokumentuj retencję w polityce prywatności; rozważ pseudonimizację/usunięcie IP powiązanych z usuniętym kontem.

## [MEDIUM] firebase/functions/src/db/repos.js:156

**usunKonto nie jest atomowe: zwalnia rezerwacje unikalności PRZED recursiveDelete — częściowa awaria osieroca konto i uwalnia e-mail**

- Scenariusz: usunKonto najpierw kasuje unique/email:{email} i unique/nip:{nip} (krok 1) oraz magic_links (krok 2), a dopiero potem robi recursiveDelete na users/{id} (krok 3). recursiveDelete nie jest transakcyjny i może paść w połowie (timeout, duża podkolekcja). Scenariusz złego szczęścia: rezerwacja e-maila już zwolniona, ale dokument użytkownika i część podkolekcji (matches/evaluations) zostają. Wtedy: (a) dane osobowe byłego użytkownika częściowo przetrwały mimo żądania z art. 17, a odpowiedź do klienta to 500 (użytkownik myśli, że usunięcie nie przeszło, choć część danych zniknęła); (b) ten sam e-mail można natychmiast zarejestrować ponownie (rezerwacja wolna), tworząc drugie konto users/{nowe_id} o tym samym e-mailu — findByEmail używa limit(1) i zwraca dowolny z dwóch dokumentów, co daje niespójne logowanie i zalegające cudze dane.
- Naprawa: Wykonaj recursiveDelete PRZED zwolnieniem rezerwacji unique, albo opakuj sekwencję tak, by uwolnienie e-maila/NIP następowało dopiero po potwierdzonym usunięciu dokumentu użytkownika; rozważ oznaczenie konta jako 'w trakcie usuwania' i retry.

## [MEDIUM] firebase/functions/src/jobs/fetchTenders.js:23

**Pobieramy tylko jedną stronę 500 ogłoszeń przy 7-dniowym oknie — ciche gubienie przetargów**

- Scenariusz: runTenderFetch woła searchNotices({page:0, size:500}) raz, bo PageNumber w BZP jest zepsuty. PublicationDateFrom liczy się jako now - BZP_LOOKBACK_DAYS (domyślnie 7 dni). W 7-dniowym oknie polski BZP publikuje znacznie więcej niż 500 ContractNotice, więc odpowiedź to tylko ~500 NAJNOWSZYCH (efektywnie ostatnie 1-2 dni). Dopóki dobowy wolumen < 500 i cron chodzi codziennie, każde ogłoszenie łapie się w dniu publikacji. Ale: (a) jeśli cron nie wykona się przez 1+ dobę (BZP down, wszystkie retry wyczerpane → job zwraca ok:false), ogłoszenia z pominiętych dni są już wypchnięte poza newest-500 i NIGDY nie wejdą do bazy; (b) w dniu ze skokiem >500 nowych ogłoszeń nadmiar przepada. Efekt niewidoczny — brak błędu, użytkownicy po prostu nie dostają części przetargów.
- Naprawa: Skoro PageNumber nie działa, ograniczyć okno (np. LOOKBACK 1-2 dni przy dziennym cronie) LUB stronicować po dacie publikacji (zawężać PublicationDateTo malejąco aż strona wróci <PageSize), a przy ok:false zapamiętać nieudany zakres dat do nadrobienia w kolejnym przebiegu.

## [MEDIUM] firebase/functions/src/db/repos.js:280

**Przetargi z deadline==null nigdy nie wygasają — wygasłe/zamknięte ogłoszenia bez pola terminu wracają jako dopasowania w nieskończoność**

- Scenariusz: Gdy ogłoszenie BZP nie ma submittingOffersDate, normalizeNotice→upsert zapisuje deadline:null (repos.js:228). openPool ma osobną gałąź `where('deadline','==',null).limit(2000)` bez żadnego kryterium wieku, a nie istnieje job czyszczący. Skutek: ogłoszenie sprzed miesięcy (dawno rozstrzygnięte) pozostaje w puli i codziennie jest scorowane oraz może tworzyć 'nowe' dopasowanie dla każdego usera pasującego słowem/CPV. Dodatkowo pula null-deadline rośnie bez ograniczeń — heuristicScore liczony na całej puli 2000 dla KAŻDEGO usera zaczyna marnować CPU na trupy, a przy >2000 nulli realnie otwarte ogłoszenia bez terminu wypadają arbitralnie (brak orderBy).
- Naprawa: Odfiltrować null-deadline po wieku: przy zapisie ustawiać zastępczy termin lub trzymać published_at/fetched_at i w gałęzi bezTerminu dodać `where('fetched_at','>', teraz - N dni)`, albo okresowo usuwać/oznaczać stare wpisy null-deadline.

## [MEDIUM] firebase/functions/src/services/bzp.js:64

**deadline z BZP zapisywany surowo i porównywany leksykograficznie z nowIso() — zakłada ISO-UTC z 'Z', czego BZP nie gwarantuje**

- Scenariusz: normalizeNotice (linia 64) bierze `submittingOffersDate` przez firstOf i zapisuje BEZ normalizacji formatu/strefy. openPool robi `where('deadline','>', nowIso())`, a nowIso() to zawsze pełne `YYYY-MM-DDTHH:mm:ss.000Z` (24 znaki). Firestore porównuje stringi bajtowo, więc porządek leksykograficzny musi = chronologiczny. Dwa realne rozjazdy: (a) BZP zwraca termin DATA-only 'YYYY-MM-DD' → przetarg z terminem DZIŚ: '2026-07-10' < '2026-07-10T14:30:00.000Z' (krótszy string jest prefiksem, więc mniejszy) → WYKLUCZONY z puli przez cały dzień, mimo że oferty można składać do końca dnia (under-inclusion trafnego przetargu). (b) BZP zwraca offset lokalny '+02:00' zamiast 'Z' → '2026-07-10T16:00:00+02:00' (=14:00 UTC, już po terminie przy now=14:30 UTC) leksykograficznie '...T16...' > '...T14:30...Z' → WŁĄCZONY mimo że termin minął (over-inclusion wygasłego). Test suite używa wyłącznie idealnego '2099-01-01T00:00:00.000Z', więc rozjazd formatu jest niewykryty.
- Naprawa: W normalizeNotice sparsuj deadline do kanonicznego UTC ISO (`new Date(raw).toISOString()`), zapisując null gdy parsowanie zawiedzie. Wtedy porównanie leksykograficzne z nowIso() jest zgodne z chronologią niezależnie od formatu/strefy zwracanego przez BZP.

## [MEDIUM] firebase/functions/src/services/bzp.js:64

**deadline zapisywany surowo z BZP i porownywany leksykograficznie z nowIso() zaklada kanoniczny UTC z Z**

- Scenariusz: normalizeNotice NIE normalizuje deadline — bierze submittingOffersDate 'as-is' (linia 64) i zapisuje do Firestore (repos upsert linia 228). Pula otwarta filtruje where('deadline','>', nowIso()), a nowIso() to zawsze new Date().toISOString() = '2026-07-10T12:34:56.789Z'. Porownanie jest LEKSYKOGRAFICZNE na stringach. Wszystkie testy uzywaja kanonicznego '2099-01-01T00:00:00.000Z', wiec zalozenie nigdy nie jest zweryfikowane na realnym formacie BZP. Jesli submittingOffersDate przychodzi jako data-only 'YYYY-MM-DD' albo ze spacja/offsetem lokalnym (np. '2026-07-10 10:00' lub '...+02:00'), to dla przetargu z terminem DZIS string '2026-07-10' < '2026-07-10T12:...' (krotszy jest prefiksem) -> przetarg z terminem konca dnia wypada z puli i nie dostaje dopasowania; a przetarg juz zamkniety o 11:00 UTC ('...T13:00+02:00') leksykograficznie '13'>'12' zostaje w puli jako otwarty. Boundary i same-day ogloszenia sa cicho odsiewane lub trzymane blednie.
- Naprawa: Znormalizowac deadline w normalizeNotice do kanonicznego ISO UTC (new Date(v).toISOString()) przed zapisem, a jesli parsowanie sie nie uda — jawnie null zamiast surowego stringa; wtedy leksykograficzne porownanie w openPool jest poprawne. Dodac test z realnym formatem submittingOffersDate (nie tylko '...Z').

## [MEDIUM] firebase/functions/src/services/bzp.js:152

**Detektor zmiany schematu BZP nie wykrywa najgorszego przypadku — odrzucenia WSZYSTKICH ogloszen**

- Scenariusz: Detektor 'podejrzenie ZMIANY SCHEMATU' jest bramkowany warunkiem `notices.length && (...)` (linia 152). Gdy BZP zmieni nazwe pola identyfikatora (np. bzpNumber/noticeNumber -> noticeIdentifier), firstOf w normalizeNotice zwraca null dla KAZDEGO ogloszenia -> `if (!externalId) return null` -> notices = [] po filter(Boolean). Wtedy notices.length == 0, wiec detektor error NIE odpala; leci tylko generyczny warn o 'odrzucone'. searchNotices zwraca [], runTenderFetch konczy z fetched:0 BEZ rzucania bledu (ok:true), a generateMatchesForAllUsers mieli na stalej/pustej puli. Efekt: najbardziej katastrofalna zmiana kontraktu (feed calkowicie martwy) przechodzi jako cichy sukces — dailyTenderFetch nie rzuca, admin nie dostaje alertu, przez co ubytek danych moze trwac dniami.
- Naprawa: Dodac galaz: gdy surowe.length > 0 a notices.length == 0 (albo odrzucone/surowe > 0.9), logowac ERROR i/lub rzucac wyjatek — wtedy dailyTenderFetch to zgłosi jak inne bledy BZP. Detektor jakosci nie moze byc bramkowany warunkiem, ktory sam znika przy totalnej awarii schematu.

## [MEDIUM] firebase/functions/src/services/matching.js:222

**Chronicznie przekraczajacy czas cron konczy sie 'sukcesem' — pominieci (platni) userzy niewidoczni w /health i Error Reporting**

- Scenariusz: generateMatchesForAllUsers po wyczerpaniu BUDZET_CZASU_CYKLU_MS (440s) robi break i loguje tylko logger.warn (linia 222 -> severity WARNING, ktore NIE tworzy incydentu w Error Reporting), po czym zwraca total. runTenderFetch dostaje matchesCreated, ustawia ok:true i zapisuje cykl.zapiszPrzebieg jako udany. /health sprawdza wylacznie swiezosc znacznika (>30h) — a znacznik jest swiezy, wiec zwraca 200 'ok'. Scenariusz: baza rosnie do ~300 kont, cron przerabia ~200 w 440s i porzuca 100 (w tym platnych 'standard'). Kazdego dnia inna setka jest glodzona (sort po last_cycle_at rotuje), user standard bywa pomijany co ~1.5 dnia i nie dostaje push/dopasowan. Zaden alert, /health zielony, Scheduler zielony — dyzurny nie ma jak proaktywnie wykryc, ze cron systematycznie nie nadaza.
- Naprawa: Gdy pominieci>0, podniesc sygnal ponad WARNING: wyemitowac zdarzenie ReportedErrorEvent (@type jak w errorHandler) i przekazac pominieci/niepelny do wyniku runTenderFetch oraz do sladu cykl.zapiszPrzebieg, zeby /health pokazywal niepelny przebieg.

## [MEDIUM] firebase/functions/src/services/bzp.js:153

**Wczesne ostrzezenie o zmianie schematu BZP nie dociera do Error Reporting — tylko linijka w logu**

- Scenariusz: Detektor zmiany schematu (linie 152-154) przy podejrzeniu przemianowania pol BZP robi wylacznie logger.error(...). Pino-owe logger.error daje wpis Cloud Logging severity=ERROR, ale BEZ pola @type ReportedErrorEvent i bez stack trace — a Error Reporting zaklada grupe/alert tylko dla wpisow z @type ReportedErrorEvent lub stackiem (co autorzy sami dokumentuja w errorHandler.js:26-32 i stosuja w zglosPrzekroczenieBudzetu). Scenariusz: BZP przemianowuje pola tytulu/CPV, feed po cichu wypelnia sie wpisami 'Bez tytulu' bez CPV, heurystyka przestaje cokolwiek trafiac, userzy przestaja dostawac dopasowania. Jedyny sygnal — logger.error — nie odpala zadnego alertu, dyzurny o 3 w nocy nie zostaje wybudzony, problem wychodzi dopiero z fali reklamacji.
- Naprawa: Wyemitowac ostrzezenie o zmianie schematu w kopercie ReportedErrorEvent (console.error z @type: ...ReportedErrorEvent), tak jak errorHandler i alarm budzetu, zeby powstal incydent/alert w Error Reporting, a nie tylko wpis w logu.

## [MEDIUM] firebase/functions/src/services/push.js:26

**sendPush ignoruje bilety Expo — nieudane pushe raportowane jako wyslane, martwe tokeny nigdy nie czyszczone i niediagnozowalne**

- Scenariusz: sendPush sprawdza tylko res.ok i zwraca sent: valid.length (linia 26), nie parsujac ciala odpowiedzi Expo (data[].status). Scenariusz: user 'standard' reinstaluje apke, stary ExponentPushToken staje sie niewazny; Expo zwraca HTTP 200 z ticketem {status:'error', details:{error:'DeviceNotRegistered'}}. sendPush raportuje sent:1, matches.markNotified oznacza dostarczone, token zostaje w profilu na zawsze. User zglasza 'nie dostaje powiadomien', a admin/users pokazuje has_push_token=true i nie ma jak stwierdzic, ze pushe od dawna sa odrzucane. Nic sie nie loguje (res.ok=true), wiec brak sladu do diagnozy.
- Naprawa: Sparsowac odpowiedz Expo (data[]); przy statusie 'error'/DeviceNotRegistered wyzerowac push_token usera w profilu i policzyc rzeczywiste sukcesy; zalogowac nieudane bilety, zeby reklamacja 'brak powiadomien' byla diagnozowalna.

## [MEDIUM] firebase/functions/src/services/push.js:26

**Odpowiedzi (receipts) Expo Push są ignorowane — martwe tokeny nigdy nie czyszczone, awarie dostarczania niewidoczne**

- Scenariusz: sendPush POST-uje do Expo i przy res.ok zwraca { sent: valid.length } NIE czytając ciała odpowiedzi. Expo na nieaktualny token (user odinstalował apkę) zwraca HTTP 200 z per-ticket status 'error' + details.error='DeviceNotRegistered' — kod tego nie widzi i raportuje sukces. Skutki: (1) push_token odinstalowanego użytkownika Standard zostaje w Firestore w nieskończoność i każdy cykl dzienny 'wysyła' na martwy adres; (2) operator nie ma ŻADNEGO sygnału, że powiadomienia się nie dostarczają — matching działa, logi mówią 'sent', a płacący klient po prostu nie dostaje pushy; (3) Expo dokumentuje, że powtarzalne słanie na DeviceNotRegistered może doprowadzić do throttlingu/zablokowania danych push — wtedy CISZA dla wszystkich naraz, bez alertu.
- Naprawa: Parsować ciało odpowiedzi Expo (data[].status/details.error). Przy DeviceNotRegistered kasować push_token usera (users.setPushToken(id, null)) i logować metrykę nieudanych dostaw jako WARNING/ERROR, żeby degradacja pushy była widoczna dla operatora.

## [MEDIUM] firebase/functions/src/services/ai.js:161

**Awaria/wygaśnięcie klucza Anthropic degraduje cały scoring do heurystyki bez żadnego alertu**

- Scenariusz: scoreTenderMatch łapie KAŻDY wyjątek z client.messages.create (401 wygasły/odwołany ANTHROPIC_API_KEY, trwałe 429, 5xx) i zwraca null → matching po cichu używa heuristicScore dla wszystkich userów. features.ai pozostaje true (klucz niepusty), budżet nieprzekroczony, więc zglosPrzekroczenieBudzetu NIE strzela. Jedyny ślad to logger.error('Wywołanie AI nie powiodło się') — zwykły log pino (bez formatu @type ReportedErrorEvent i bez progu), którego nikt nie ogląda. Admin /ai-budget i /stats pokazują zdrowy budżet. Produkt tygodniami serwuje gorsze dopasowania (scorer=heuristic) i nikt się nie dowiaduje.
- Naprawa: Zliczać kolejne niepowodzenia AI i po progu (albo dla 401/403) emitować ReportedErrorEvent jak zglosPrzekroczenieBudzetu; ewentualnie wystawić licznik scorer=ai/heuristic w /admin/stats.

## [MEDIUM] firebase/functions/src/services/push.js:26

**Ignorowane bilety/paragony Expo — martwe tokeny nigdy nie wykrywane, notified=1 mimo braku dostarczenia**

- Scenariusz: sendPush uznaje wysyłkę za udaną na podstawie samego HTTP res.ok i zwraca {sent: valid.length}, nie czytając biletów odpowiedzi Expo. Expo przy HTTP 200 zwraca per-wiadomość ticket ze status:'error' i details.error:'DeviceNotRegistered' dla tokenów po odinstalowaniu/cofnięciu zgody. matching.js (linia ~158) i tak woła markNotified(user.id, m.id) niezależnie od wyniku (nawet przy {sent:0}). Skutek: subskrybent Standard, który przeinstalował apkę lub cofnął zgodę, po cichu przestaje dostawać powiadomienia, jego martwy push_token nigdy nie jest czyszczony, a dopasowania są oznaczone jako 'powiadomione' — brak sygnału operacyjnego, że dostarczanie push nie działa dla tego konta.
- Naprawa: Odczytać bilety z odpowiedzi Expo; dla DeviceNotRegistered wyczyścić users.push_token; markNotified wywoływać tylko dla realnie wysłanych; zliczać błędy do alertu.

## [MEDIUM] firebase/functions/src/routes/webhooks.js:109

**Handlery webhooka (aktywacja Standard, brama payment_status, faktura/e-mail) bez zadnego testu**

- Scenariusz: obsluzCheckout (109), obsluzZmianeSubskrypcji (156) i obsluzUsuniecieSubskrypcji (175) nie sa wywolywane przez zaden test - stripeEvents.test.js pokrywa wylacznie hydraulike claim/markDone/release. Sciezka aktywacji czyta userId z `session.client_reference_id || session.metadata?.user_id`, sprawdza `payment_status !== 'paid'`, wola setStripeCustomer/setStripeSubscription/setTier i wystawia fakture. Scenariusz: regresja w tej sciezce (np. zla nazwa pola id, pominiete setTier, odwrocony warunek payment_status) sprawia, ze oplacony klient nigdy nie dostaje planu 'standard' albo dostaje go bez zaplaty - i nic tego nie wykryje przed wdrozeniem.
- Naprawa: Dodaj test emulatorowy wywolujacy handleEvent z zamockowanym obiektem event dla checkout.session.completed / async_payment_succeeded / subscription.updated / deleted i asertujacy realna zmiane tieru i wywolanie faktury.

## [MEDIUM] firebase/functions/src/routes/webhooks.js:19

**Rozjazd backend vs firebase: regula uprawnien nie zostala przeniesiona jako wspoldzielony testowany modul**

- Scenariusz: backend wydzielil regule do lib/subscriptionStatus.js i testuje REALNA funkcje tierPrzetargAi w webhookStatus.test.js; firebase wkleil ja inline jako goly Set (webhooks.js:19) z testem sprawdzajacym tylko kopie. Oba drzewa rozchodza sie na statusach spoza listy: backend tierPrzetargAi('incomplete') -> 'standard' (bo 'incomplete' nie jest w BEZ_DOSTEPU), firebase -> 'free' (bo 'incomplete' nie jest w UPRAWNIA_DO_STANDARD). W oknie migracji Railway->Firebase oba webhooki moga przetwarzac te sama subskrypcje PrzetargAI, dajac niespojne uprawnienie dla statusow 'incomplete'/'trialing', a tylko galaz backendu ma test pilnujacy reguly.
- Naprawa: Wspoldziel jedna implementacje reguly (lub zduplikuj wraz z testem importujacym realny symbol) i ujednolic traktowanie statusow spoza listy miedzy backend/ a firebase/.

## [MEDIUM] firebase/functions/src/services/ai.js:123

**Cala galaz AI silnika dopasowan jest martwa w testach — ai.js bez testow, klucz zawsze wyzerowany**

- Scenariusz: Kazdy test dotykajacy silnika (matching.test.js:22, zdrowie.test.js:4) ustawia `process.env.ANTHROPIC_API_KEY = ''`, wiec `features.ai=false` i `client=null`. Skutkiem `scoreTenderMatch` zawsze zwraca null na wejsciu (linia 124), a w matching.js `wartoPytacAi` jest zawsze false — nietestowane pozostaja: obliczenie `aiBudget = min(AI_RERANK_TOP_N, limitDobowy - zuzytoDzis)`, wpiecie `aiQuota.reserve` w petle, sciezka gdy AI zwraca wynik (`aiOdpowiedzialo=true`, nadpisanie score/reasoning/scorer) oraz `ocenaOstateczna` po realnej odpowiedzi AI. Sam `ai.js` (parseScore z clampingiem 0-100 i wycinaniem reasoning, budgetStatus, alarmy budzetu, zapis aiUsage z costUsd) nie ma ZADNEGO pliku testowego. Test 'denial-of-wallet' (matching.test.js:115) woła aiQuota.reserve BEZPOSREDNIO, nie przez silnik, wiec nie chroni wpiecia w petli. Scenariusz: regresja w linii 108-119 (np. brak dekrementacji aiBudget albo zle parsowanie JSON w parseScore) przechodzi wszystkie testy.
- Naprawa: Dodac testy jednostkowe parseScore (JSON w tekscie, wartosci spoza 0-100, brak reasoning) oraz test integracyjny silnika ze zamockowanym klientem Anthropic (features.ai=true), pokrywajacy budzet AI i fallback po bledzie AI.

## [MEDIUM] firebase/functions/src/services/invoice.js:4

**Faktura VAT wystawiana zawsze na 199 zl netto niezaleznie od kwoty realnie pobranej przez Stripe**

- Scenariusz: createStandardInvoice hardkoduje `STANDARD_NET_PLN = 199` i wystawia fakture na gross 244,77 zl, nigdy nie odczytujac kwoty z sesji/faktury Stripe. Checkout w stripe.js:29 ma `allow_promotion_codes: true`, a cena to osobny obiekt `STRIPE_PRICE_STANDARD`. Scenariusz A: klient uzywa kodu rabatowego (np. -50%), Stripe pobiera ~99 zl, a Fakturownia wystawia fakture na 199 netto/244,77 gross — faktura nie zgadza sie z platnoscia (problem ksiegowo-podatkowy). Scenariusz B: cena w Stripe zostaje podniesiona do 249 zl, kod dalej wystawia 199 netto -> zanizony VAT. Brak jakiegokolwiek testu invoice.js.
- Naprawa: Odczytywac faktyczna kwote (amount_total/amount_paid) z obiektu sesji lub faktury Stripe zamiast hardkodowac 199; ewentualnie pobierac cene z jednego zrodla prawdy.

## [MEDIUM] firebase/functions/src/db/repos.js:280

**openPool ucina pule do 2000 — najnowsze przetargi wypadaja z matchingu przy skali**

- Scenariusz: openPool(limit=2000) sklada dwie galezie. Galaz z terminem: `.where('deadline','>',now).orderBy('deadline').limit(2000)` zwraca 2000 NAJWCZESNIEJ wygasajacych. Galaz bez terminu: `.where('deadline','==',null).limit(2000)` — BEZ orderBy, wiec Firestore zwraca po __name__ rosnaco, a docId to zsanityzowany numer BZP (rosnacy chronologicznie), czyli 2000 NAJSTARSZYCH przetargow bez terminu. BZP (referencja: wartosc/deadline czesto brak) latwo utrzymuje >2000 otwartych ogloszen. Skutek: firma z idealnym dopasowaniem do swiezo dodanego przetargu z dalekim terminem (lub bez terminu) NIGDY nie zobaczy go w feedzie, dopoki starsze/wczesniej wygasajace nie wypadna z czolowki 2000. Cron i backfill uzywaja tej samej puli, wiec luka dotyczy calego produktu. Emulator z mala baza tego nie ujawni.
- Naprawa: W galezi deadline==null dodac orderBy('fetched_at','desc') (wymaga indeksu zlozonego — jawnie dodac do firestore.indexes.json) albo przetwarzac pule strumieniowo/wsadowo zamiast twardego limitu 2000; rozwazyc usuwanie/archiwizacje przetargow po terminie, by pula nie rosla bez ograniczen.

## [MEDIUM] firebase/functions/test/indeksyFirestore.test.js:58

**Straznik indeksu collectionGroup nic nie sprawdza — czyta zly plik**

- Scenariusz: Test 'collectionGroup ma zadeklarowany indeks' skanuje WYLACZNIE src/db/repos.js (stala REPOS). Jedyne uzycie collectionGroup('matches') w kodzie jest w src/routes/admin.js (/recent-matches). W repos.js nie ma zadnego collectionGroup, wiec `uzycia.length===0` i test robi `return` bez zadnej asercji — przechodzi na zielono nie weryfikujac niczego. Realne zapytanie collectionGroup w admin.js jest wiec niestrzezone; gdyby ktos dodal kolejne collectionGroup w dowolnym pliku tras (albo usunal wpis z firestore.indexes.json), test tego nie zlapie, a produkcja rzuci FAILED_PRECONDITION przy pierwszym wywolaniu (emulator indeksow zlozonych/collection-group nie egzekwuje — dokladnie klasa bledu, przed ktora ten test mial chronic).
- Naprawa: Skanowac wszystkie pliki zrodlowe (src/**), nie tylko repos.js, przy wykrywaniu collectionGroup(...) oraz where/orderBy — np. glob po src i konkatenacja tresci przed matchowaniem wzorcow.

## [MEDIUM] codemagic.yaml:88

**CI buduje i wysyla apke mobilna oraz wdraza funkcje bez uruchomienia zadnych testow**

- Scenariusz: codemagic.yaml (workflowy iOS i Android) wykonuje tylko npm ci -> expo prebuild -> podpis -> build -> submit_to_testflight/Play. Brak kroku `npm test` (mobile/test/apiErrors.test.js) i brak `npm run check` (skladniowy esbuild). Regresja lub blad skladni w mobile/src trafia prosto do TestFlight/Play. Dla funkcji nie ma zadnego CI (brak .github/workflows), a `npm run deploy` to gole `firebase deploy` — nic nie wymusza testow przed wdrozeniem backendu obslugujacego platnosci i dane RODO.
- Naprawa: Dodac do obu workflowow krok testowy przed buildem (cd mobile && npm run check && npm test) oraz osobny pipeline/pre-deploy hook dla funkcji uruchamiajacy npm run test:emu; blokowac deploy przy niepowodzeniu.

## [LOW] firebase/functions/src/routes/webhooks.js:76

**Blad markDone moze doprowadzic do podwojnej faktury/e-maila przy pozniejszej powtornej dostawie Stripe**

- Scenariusz: obsluzCheckout konczy sie sukcesem (setTier + faktura Fakturownia + e-mail), po czym stripeEvents.markDone rzuca (chwilowy blad Firestore). Zwracamy 200, wiec rezerwacja zostaje w stanie 'processing' i wygasa po 5 min (REZERWACJA_WYGASA_PO_MS). Jesli Stripe dostarczy to samo event.id ponownie po >5 min (Stripe gwarantuje dostawe co najmniej raz, zdarzaja sie duplikaty), claim() widzi wygasla rezerwacje i przetwarza zdarzenie DRUGI RAZ: createStandardInvoice tworzy druga fakture, sendEmail wysyla drugi e-mail — dokladnie to, przed czym mial chronic rejestr idempotencji. Operacje NIE sa idempotentne mimo komentarza.
- Naprawa: Uczynic domkniecie odpornym: ponawiac markDone kilka razy z backoffem przed zwrotem 200, lub zwrocic 500 gdy markDone zawiedzie (Stripe ponowi, a claim w oknie <5min i tak odrzuci duplikat), albo oznaczac done w tej samej transakcji/batchu co skutki uboczne.

## [LOW] firebase/functions/src/services/ai.js:32

**30s budgetStatus cache makes the 'twardy' AI budget limit non-binding for up to 30s of calls**

- Scenariusz: cacheBudzetu is module-level and TTL 30s. During the daily cron (single warm instance) scoreTenderMatch is called repeatedly; the first call caches hardExceeded=false. If monthly spend crosses AI_BUDGET_HARD_USD mid-cycle, every subsequent scoreTenderMatch for up to 30s keeps seeing the stale hardExceeded=false and keeps calling the model (5 users in parallel × ~1 call/1.5s ≈ ~100 calls before the cache refreshes and finally blocks). Overrun is bounded (~one 30s window of Haiku calls, cents) and self-heals on the next refresh, but the 'hard' limit is not actually hard within the window. From a 3am on-call view: a cost spike won't be stopped instantly by the hard cap; it's visible in logs but not prevented.
- Naprawa: When status.hardExceeded is about to be relied on to block, re-check with budgetStatus({ swiezy: true }) (or shorten TTL / bypass cache once softExceeded is true), so the hard cap is evaluated against live spend.

## [LOW] firebase/functions/src/screens/MatchFeedScreen.js:44

**Concurrent pull-to-refresh + in-flight load-more corrupts cursor and produces duplicate FlatList keys**

- Scenariusz: User scrolls, onEndReached fires dociagnijWiecej which passes its guard and starts fetching page 2 with before=cursorA. Before it resolves, the user pulls to refresh: load('refresh') sets refreshing, replaces matches with a fresh page 1 and sets kursor=newCursor. The still-in-flight load-more then runs setMatches(prev => [...prev, ...page2]) appending the OLD page-2 to the NEW page-1 list, and (having overwritten nothing) the next onEndReached uses newCursor (= page-1 tail) and re-fetches page 2 again → the same match ids appear twice → React 'duplicate key' warning and duplicated cards in the feed. The guard only checks refreshing at call-time; it does not cancel/ignore an already in-flight load-more when a refresh starts.
- Naprawa: Tag each load with a request id / sequence and ignore stale load-more results whose sequence predates the last refresh; or drop appended results when a refresh has occurred since the load-more began (e.g., check a ref set at refresh start).

## [LOW] firebase/functions/src/routes/auth.js:193

**Brak endpointu eksportu danych (RODO art. 20) mimo obietnicy przenoszenia danych w polityce prywatności**

- Scenariusz: Polityka prywatności (legal.js §7) deklaruje prawo do 'przenoszenia danych' i 'otrzymania kopii', ale w API istnieje tylko DELETE /auth/me (usunięcie) i GET /auth/me (bieżący profil) — brak jakiegokolwiek endpointu eksportu pełnego zestawu danych użytkownika (profil + matches + evaluations + feedback + historia audytu). Użytkownik żądający kopii/przeniesienia danych nie ma technicznej ścieżki; obsługa musi robić to ręcznie z konsoli Firestore, co przy większej liczbie kont jest nieskalowalne i podatne na błąd (pominięcie podkolekcji). To luka zgodności względem zadeklarowanego prawa.
- Naprawa: Dodaj GET /auth/me/export zwracający komplet danych użytkownika (profil + subkolekcje) w formacie do przeniesienia (JSON), analogicznie do zakresu usunKonto.

## [LOW] mobile/src/config.js:19

**Produkcyjny API_URL aplikacji mobilnej wskazuje backend Railway, który po migracji D-024 obsługuje już tylko Fittera**

- Scenariusz: API_URL dla builda produkcyjnego jest zahardkodowany na https://backend-production-a43e3.up.railway.app. Po pełnej migracji backendu PrzetargAI na Firebase (D-024), gdzie Railway ma obsługiwać już tylko Fittera, produkcyjny build aplikacji uderzałby w backend bez tras /auth/*, /matches/* dla PrzetargAI — cała aplikacja przestaje działać w najgorszym momencie (po przełączeniu ruchu na Firebase). Komentarz w pliku sam sygnalizuje, że adres jest placeholderem do zmiany. UWAGA: jeśli migracja nie została jeszcze wdrożona (Railway wciąż serwuje przetargai), to na dziś adres jest poprawny — defekt materializuje się dopiero po deployu Firebase i wygaszeniu tras przetargai na Railway.
- Naprawa: Przed publikacją builda produkcyjnego zmień API_URL na URL Cloud Functions (lub docelową domenę api.przetargai.pl) i najlepiej wyprowadź go z konfiguracji środowiska zamiast twardego wpisu.

## [LOW] firebase/functions/src/lib/textNorm.js:97

**matchKeywords re-stemuje słowa kluczowe profilu dla KAŻDEGO przetargu — zbędny koszt CPU w cyklu na 2000×N**

- Scenariusz: heuristicScore woła matchKeywords(keywords, title) raz na parę (user, tender). Wewnątrz pętli (linia 97) `stemTokens(keyword)` liczy się od nowa dla każdego słowa kluczowego przy każdym tytule. generateMatchesForUser mapuje heurystykę po CAŁEJ puli (do 2000) dla każdego usera: user z 8 słowami → 8×2000 = 16 000 zbędnych stemowań słów kluczowych na usera, ×N userów w cyklu. To czysto redundantne (rdzenie słów kluczowych są niezmienne w obrębie usera). Przy dużej puli i wielu userach zjada budżet 440s cyklu, co realnie zmniejsza liczbę obsłużonych userów przed przerwaniem (ogon listy głodzony mimo mechanizmu last_cycle_at).
- Naprawa: Wstępnie zestemuj słowa kluczowe raz na usera (np. `matchKeywords` przyjmuje już-zestemowane parts, albo memoizacja per-user w generateMatchesForUser) i przekazuj do heurystyki, zamiast liczyć stemTokens(keyword) dla każdego z 2000 przetargów.

## [LOW] firebase/functions/src/routes/health.js:50

**/health publicznie ujawnia wersję wdrożenia i dzienne metryki biznesowe, bez auth i bez rate-limitu**

- Scenariusz: W app.js `/health` jest zamontowany PRZED jakimkolwiek limiterem i bez uwierzytelnienia. Odpowiedź zawiera `wersja` (process.env.K_REVISION — konkretna rewizja wdrożonego kodu, ułatwia rekonesans pod znane podatności) oraz `cron.ostatni_wynik`, które jest CAŁYM obiektem `wynik` z fetchTenders: fetched, newTenders, matchesCreated, skipped, durationMs. To jawne, publiczne dane o aktywności i skali biznesu (ile przetargów/dopasowań dziennie ≈ proxy liczby i aktywności klientów). Dodatkowo każde uderzenie w /health robi 2 odczyty Firestore (_health ping + cykl.ostatniPrzebieg) — nielimitowane, więc daje się bić w pętli jako drobny denial-of-wallet i generator inwokacji funkcji.
- Naprawa: Zwracać publicznie tylko status/db/cron.ok/godzin_temu; `wersja` i szczegółowy `ostatni_wynik` udostępniać wyłącznie za nagłówkiem admina lub w /admin. Rozważyć lekki rate-limit/cache na /health.

## [LOW] mobile/src/api/client.js:47

**Krytyczna logika wygasniecia sesji (401 -> globalny wylogowanie) bez testu**

- Scenariusz: mobile/src ma tylko apiErrors.test.js. Warunek client.js:47 `if (err.wygaslaSesja && auth && authToken && naWygasnieciesesji) naWygasnieciesesji()` steruje globalnym wylogowaniem, ktore AuthContext podpina przez onSesjaWygasla (AuthContext.js:87). Zaden test nie sprawdza tej sciezki. Scenariusz: refaktor upraszcza warunek (np. usuwa `auth && authToken`), przez co 401 na publicznej trasie logowania ('zle haslo') wywoluje globalny signOut/petla wylogowania; albo odwrotnie - 401 na trasie z tokenem przestaje wylogowywac i apka wisi na ekranie bledu. Regresja przechodzi bez wykrycia, bo `npm test` (node --test test/*.test.js) nie ma czego uruchomic.
- Naprawa: Dodaj test request()/AuthContext z zamockowanym fetch zwracajacym 401 na trasie auth=true vs auth=false i asertuj wywolanie/niewywolanie handlera wygasniecia sesji.

## [LOW] mobile/src/api/client.js:47

**Logika wygasania sesji i caly mobile/src praktycznie bez testow**

- Scenariusz: mobile/src ma 17 plikow logiki (client.js, AuthContext.js, format.js, storage.js, push.js, ekrany) i tylko JEDEN plik testowy (apiErrors.test.js). Warunek auto-wylogowania w client.js:47 `if (err.wygaslaSesja && auth && authToken && naWygasnieciesesji)` nie jest pokryty testem. Scenariusz: regresja upraszczajaca ten warunek (np. usuniecie `auth && authToken`) spowoduje globalne wylogowanie uzytkownika przy 401 z trasy publicznej (np. bledne haslo przy logowaniu) — dokladnie regresja, ktora ten warunek naprawil (audyt 2026-07-09) — i nic tego nie wykryje. Podobnie nietestowane: format.daysUntil (ceil daje 'za 1 dni' dla terminu za kilka godzin), paginacja kursorem w MatchFeedScreen.
- Naprawa: Dodac testy jednostkowe dla client.js (mock fetch: 401 na trasie auth vs public), format.js (edge case daysUntil/formatBudget) i redukcji stanu AuthContext.

## [LOW] firebase/functions/package.json:6

**Domyslne `npm test` pomija cala warstwe danych — zielono mimo nieprzetestowanej integracji**

- Scenariusz: Wszystkie testy integracyjne (firestoreRepos, matching, usuniecieKonta, zdrowie, oraz emulatorowe czesci) maja `{ skip }` gdy brak FIRESTORE_EMULATOR_HOST. Domyslne `npm test` (to co odpali developer i co nie startuje emulatora) POMIJA repos.js, silnik matchingu, usuwanie konta RODO, health i idempotencje webhooka — raportujac 'pass'. Realnie pod plain npm test biegna glownie czyste funkcje lib oraz testy zrodlowe (z ktorych indeksyFirestore ma pusta galaz collectionGroup, a stripeEvents testuje kopie reguly). Tylko `npm run test:emu` cwiczy integracje, a nic (brak CI funkcji, deploy=bare firebase deploy) nie gwarantuje jego uruchomienia. Developer widzi 'testy przechodza' i wdraza regresje repos/matching/webhook.
- Naprawa: Uczynic emulator warunkiem sukcesu: w skrypcie `test` wykrywac brak FIRESTORE_EMULATOR_HOST i konczyc bledem z instrukcja `npm run test:emu`, albo ustawic `test` = `test:emu` i wydzielic czyste testy jednostkowe do osobnego skryptu.

